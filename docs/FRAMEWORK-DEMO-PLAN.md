# 现有框架 Demo 迁移 · 2026-09-21

用户已授权按建议执行：用现有 Vue 3 / Element Plus / Art Design Pro 前端做独立 Demo，替代纯HTML复刻作为后续评审入口。

## 目标与边界
直接在 hengxin-smart-image/frontend 复用既有路由、Art布局/侧栏/页签、Element Plus表单/表格/Drawer/Dialog/图片预览与业务组件。新建显式 mode=demo、专用3010端口及dist-demo构建；真实默认构建仍调用真实API，不能故障回落Demo。旧HTML原型已在框架Demo验收后清理，正式前端与本计划是唯一维护源。

## 有序交付
1. 复用既有Mock服务，增加仅Demo使用的IndexedDB持久化和真实本地示例图；保存workspace、上传文件、模板历史、结果槽位/版本/轮次、用户/配置/默认Skill。刷新继续同一模拟轮次，避免虚构新版本；失败保存明确报错。普通mock测试保持内存行为。
2. 将图片优化迁入真实业务组件：共用ElImage/ElImageViewer，缩略图contain、鼠标点击整组预览、左右键/按钮、缩放旋转拖动、Esc返回；当前与历史版本分组明确。模板最多4张缩略图+总数，可看全部，选择与看图分离。
3. 创建页选中模板收起为摘要、可更换，上传入口可见；不可用能力前置。详情保留现有ElDrawer，结果与失败恢复在前，输入与绑定收起，日志在后。模板编辑/历史/素材/成品沿用组件实现。
4. Demo保留现有模板CRUD、三类任务、单图/整套返工、下载归档、角色、统计、监控、成员、Skill及配置页面；将角色/场景切换和重置改为明确的Demo控制。切换角色保留同场景数据；场景数据按场景键隔离，重置只清对应Demo存储。
5. 验证typecheck、现有全量单测、demo与production构建、独立context浏览器闭环、刷新持久化、图集和角色、1440/1280截图、网络记录确认Demo不请求真实API；独立审查并提供本地运行地址与可分享构建包。

## 文件清单
新增 api/hengxin/demo-{state,storage,service}.ts、业务图片公共组件；调整client.ts显式模式、Mock层可选快照钩子、对应业务Vue和布局样式、package.json启动构建命令。仍使用当前锁定依赖，不换框架、不升级库。Demo 本身仅本地运行；后续正式前端 UI 发布按 2026-09-22 用户授权和 DEV-PLAN 执行。

## 验收
浏览器真实DOM出现现有Art侧栏/头部/工作页签和Element Plus组件；保留既有业务合同（结果数量跟随模板，文字跟随素材；归档为各槽位当前版本集合；历史图仅查看/单图下载）。示例不是AI真实结果，身份/服务均为Demo。正式生产构建不启用Mock/Demo，独立测试不写用户演示数据。

## 审查修正

独立审查发现不兼容本地数据时初始化失败会阻断重置。已将Demo重置入口加入启动失败页，重置先停止本页服务、重新取得当前场景锁后才删除；初始化失败释放锁，重新连接可重新尝试。故障测试覆盖不兼容schema恢复和另一标签页禁止重置。
