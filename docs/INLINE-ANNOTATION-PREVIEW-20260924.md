# 单张修改直接标注 · 本地交互预览

最终澄清与当前范围：只有一个编辑画布，工具选择只保留框选/画笔。删除第三个平移模式，画布内常驻“拖动图片”手柄，放大后按住拖动手柄即可移动整张图片，原标注、缩放和选中工具保持；100%无可移动空间时明确提示先放大。验收不按任何快捷键，分别完成框选→拖动手柄→框选、画笔→拖动手柄→画笔，以及平移边界/失焦取消/草稿/合成。以下三模式或快捷键主路径条目为被替代的历史方案。

最新追加：用户要求框选/画笔/平移在同一画布顺畅共存。左键执行所选工具，空格+左键或中键拖动临时平移，松开后继续原标注工具。常驻提示与手形状态；意见输入区空格正常输入，窗口失焦清除临时状态，指针取消不残留半笔。仅增量预览，不自动切换放大工具。覆盖此前“不增加快捷平移”的限制。

2026-09-24追加：用户明确保留原平移/缩放交互，仅增加画笔圈注。画笔按下到松开为一笔，自动编号并填写该处意见；可与方框混用，撤销/选中删除/清空/草稿/合成预览一致支持笔迹，坐标跟随图片缩放。验收画笔、混合导出与原框选/平移回归。仍仅预览。

1. 继承现有蓝色操作主题、白色圆角弹窗、当前版本提示，隔离页面放在hengxin-smart-image/previews/inline-annotation，验证证据在output/annotation-preview-20260924。API与CLI切换、居中大图/右侧意见、小屏上下排版。
2. 实现矩形框选与编号、选择后拖动/右下角调整大小、缩放和平移、撤销/删除/清空；每框独立意见及整体补充。以图片坐标保存框，不因视口缩放漂移。草稿在本次页面会话按入口保留。
3. 直接标注与上传已有标注图互斥，上传支持JPG/PNG/WebP、10MiB、拖拽/聚焦粘贴。生成一张带编号的标注图供提交前查看，意见同步展示；明确预览不生成、不连接生产、不保证框外逐像素不变。
4. 浏览器测试实际拖动框选、移动调整、撤销删除、缩放坐标保持、两个入口、上传与提交预览；检查1440×900和窄屏。独立审查后展示本地地址。用户确认之前不修改业务源码、不部署。

这是方案预览，演示图片来自现有项目demo素材；模拟提交不会产生业务记录。首版不保存跨浏览器会话草稿，不模拟真实生成结果。

## 当前验证

单画布最终版：`node --check`及single-canvas-check.mjs、pen-check.mjs、combined-check.mjs通过。实测只存在一个canvas与两个工具选择，无pan模式按钮；不使用快捷键，分别在框选和画笔选中时按住图中手柄移动，再继续标注。图片视口变化，笔迹/方框坐标及工具/缩放保持；合成、边界、原比例禁用说明、方向键辅助和窄屏均通过，page errors=0。截图single-canvas.png，入口?v=5-single-canvas。历史check.mjs含已删除pan模式按钮的断言，不适用新版；其平移回归已由single-canvas-check替代。

临时平移增量：语法检查、combined-check.mjs及既有pen-check.mjs/check.mjs全部通过。实测放大后框选→空格平移→继续框选、画笔→中键平移→继续画笔，标注坐标/数量不变、工具不切换、文字空格正常、窗口失焦重置及窄屏无横溢出。常驻说明截图combined-desktop.png。新版预览URL参数v=4-combined。

画笔增量：`node --check`、既有 `check.mjs`、新增 `pen-check.mjs` 均通过。新增浏览器验证自由圈注与方框混合、每笔意见、缩放不切工具且坐标保持、删除/撤销、导出PNG实际橙色笔迹像素、API入口画笔、窄屏无横向溢出及无脚本错误。截图pen-desktop.png、pen-submit.png位于同证据目录。原有缩放与平移分支保持。

2026-09-24：`node --check hengxin-smart-image/previews/inline-annotation/app.js` 通过。`node output/annotation-preview-20260924/check.mjs` 浏览器验证通过：连续框选/编号意见、缩放后坐标保持、移动/右下调整大小、撤销/删除/确认清空再恢复、平移、合成图片及模拟提交、关闭重开草稿、API/CLI草稿隔离、上传/移除/必填提示，390px视口无水平溢出；page errors=0。截图desktop.png、submit.png、mobile.png位于同证据目录。预览地址 http://127.0.0.1:3018/ 。正式业务源码与生产没有修改。
