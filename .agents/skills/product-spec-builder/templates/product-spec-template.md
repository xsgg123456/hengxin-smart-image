---
name: product-spec-template
description: Product Spec 输出模板。当需要生成产品需求文档时，按照此模板的结构和格式填充内容，输出为 Product-Spec.md 文件。
related_docs: design-brief.md
---

# Product Spec 输出模板

本模板用于生成结构完整的 Product Spec 文档。section 对应需求采集的七个 Phase，按采集到的内容填，没采集到的标 [待补充]，不凭空补。

---

# 产品需求规范：{{产品名称}}

## 0. AI 使用说明

- 本文档是产品功能、范围、行为和验收标准的事实来源。
- AI MUST 优先实现 P0。
- AI MUST NOT 实现“不在本版本范围”中明确排除的内容。
- AI MUST 根据“验收标准”判断功能是否完成。
- 如果信息不明确，AI MUST 使用“假设”中的假设；如果仍无法判断，应记录到“待确认问题”，而不是自行扩展需求。

---

## 1. 产品上下文

### 1.1 产品摘要

{{用 1-3 句话说明这个产品是什么。}}

### 1.2 用户问题

{{用户现在遇到的核心问题是什么？}}

### 1.3 目标用户

| 用户类型 | 描述 | 核心需求 |
|---|---|---|
| {{用户类型}} | {{用户是谁}} | {{他们最想完成什么}} |

### 1.4 核心价值

{{这个产品给用户带来的核心价值。}}

### 1.5 成功标准

| 判断标准 | 目标 / 信号 |
|---|---|
| {{成功标准}} | {{如何判断产品有效}} |

---

## 2. 范围

### 2.1 本版本范围

| 编号 | 内容 | 优先级 | 备注 |
|---|---|---|---|
| SCOPE-001 | {{本版本要做的内容}} | P0 | {{说明}} |

### 2.2 不在本版本范围

| 编号 | 内容 | 原因 |
|---|---|---|
| OUT-001 | {{本版本不做的内容}} | {{原因}} |

---

## 3. 用户任务

| 编号 | 用户任务 | 用户类型 | 优先级 |
|---|---|---|---|
| TASK-001 | {{用户要完成什么任务}} | {{用户类型}} | P0 |

---

## 4. 用户流程

### FLOW-001: {{流程名称}}

**关联任务：** TASK-001\
**优先级：** P0\
**目标：** {{这个流程帮助用户完成什么}}

**入口：**\
{{用户从哪里开始这个流程}}

**主路径：**
1. {{步骤 1}}
2. {{步骤 2}}
3. {{步骤 3}}

**分支路径：**
- {{分支流程}}

**边界情况：**
- {{边界情况}}
- {{异常情况}}

**完成状态：**
{{流程完成后，用户看到什么，系统发生什么}}

---

## 5. 功能需求

### REQ-001: {{功能名称}}

**优先级：** P0\
**关联任务：** TASK-001\
**关联流程：** FLOW-001\

**用途：**\
{{这个功能为什么存在。}}

**行为：**\
{{这个功能具体怎么工作。}}

**规则：**
- MUST {{必须遵守的规则}}
- MUST {{必须遵守的规则}}
- SHOULD {{建议遵守的规则}}

**输入：**

| 字段 | 类型 | 必填 | 校验规则 |
|---|---|---:|---|
| {{字段名}} | {{类型}} | Yes/No | {{校验规则}} |

**输出 / 结果：**
- {{系统输出什么}}
- {{用户看到什么结果}}

**状态：**
- 默认：{{默认状态}}
- 加载：{{加载状态}}
- 空状态：{{空状态}}
- 错误：{{错误状态}}
- 成功：{{成功状态}}

**验收标准：**
- [ ] AC-001: Given {{前置条件}}, when {{用户行为}}, then {{预期结果}}.
- [ ] AC-002: {{可测试的完成条件}}
- [ ] AC-003: {{边界情况或错误情况的验收条件}}

### AI 能力规格（每个 AI 功能必填）

| AI 功能 | 能力类型 | 质量条 | 触发方式 | 不确定时 | 服务降级 |
|---|---|---|---|---|---|
| {{功能}} | {{生成 / 理解 / RAG / agent}} | {{命中率阈值与报警线}} | {{建议 or 自动}} | {{说不确定 / 给出处 / 转人工}} | {{禁用 / 降级缓存 / 排队}} |

**AI 护栏（绝不能做）：**
- {{逐条列出 AI 绝不能做的事，哪怕用户要求；以及它能犯的最贵的错和怎么防}}

> AI 是概率性的，质量条、触发方式、护栏不钉死，开发就是在赌。

---

## 6. 数据模型

### 6.1 核心实体

| 实体 | 描述 | 关键字段 |
|---|---|---|
| {{实体名}} | {{实体含义}} | {{关键字段}} |

### 6.2 实体关系

| 关系 | 描述 |
|---|---|
| {{A belongs to B}} | {{说明}} |

### 6.3 数据规则

- {{数据创建规则}}
- {{数据更新规则}}
- {{数据删除或归档规则}}
- {{权限或可见性规则}}

---

## 7. 外部依赖

| 编号 | 依赖 | 用途 | 是否必需 | 备注 |
|---|---|---|---:|---|
| DEP-001 | {{第三方服务/API/模型/库}} | {{用途}} | Yes/No | {{说明}} |

---

## 8. 非功能需求

| 类别 | 要求 | 优先级 |
|---|---|---|
| 性能 | {{性能要求}} | P0/P1 |
| 安全 | {{安全要求}} | P0/P1 |
| 隐私 | {{隐私要求}} | P0/P1 |
| 兼容性 | {{设备、浏览器、平台要求}} | P0/P1 |
| 可靠性 | {{错误恢复、数据保存、失败重试要求}} | P0/P1 |
| 可访问性 | {{基础可访问性要求}} | P1 |

---

## 9. 完成定义

MVP 完成条件：

- [ ] 所有 P0 requirements 已实现
- [ ] 所有 P0 acceptance criteria 已通过
- [ ] 所有 P0 user flows 可以端到端完成
- [ ] 主要错误状态、空状态、加载状态已处理
- [ ] Product Spec 和 Design Spec 中的 P0 内容保持一致

---

## 10. 假设与待确认问题

### 10.1 假设

| 编号 | 假设 | 假设依据 | 错误风险 |
|---|---|---|---|
| ASM-001 | {{当前假设}} | {{为什么这样假设}} | {{如果错了会怎样}} |

### 10.2 待确认问题

| 编号 | 问题 | 是否阻塞 | 备注 |
|---|---|---:|---|
| Q-001 | {{待确认问题}} | Yes/No | {{说明}} |

---

## 11. Agent 系统规格（仅 agent 产品填）

> 产品本身是 agent / 自主系统（用户给目标、系统自主执行）时填本节；传统界面应用跳过。

### 11.1 自主性与人在回路

| 动作类别 | 自主级别 | 审批 / 回滚 |
|---|---|---|
| {{动作}} | {{自动 / 建议确认 / 禁止}} | {{是否需人确认、能否回滚}} |

### 11.2 工具与能力集

| 工具 / 能力 | 用途 | 权限级别 | 扩展机制 |
|---|---|---|---|
| {{工具}} | {{用途}} | {{读 / 写 / 执行}} | {{MCP / plugin / 无}} |

### 11.3 上下文与记忆

- 单任务上下文上限与超限处理：{{压缩 / 分段 / 检索}}
- 跨会话记忆：{{记什么、记多久、存哪}}

### 11.4 编排与多 agent

- {{单 agent 扛到底 / 拆子 agent}}；{{隔离与结果合并方式}}

### 11.5 评估与可观测（Eval）

- 评估方式：{{评估集 / 指标 / 回归}}
- 可观测：{{每步 tracing、出错怎么定位}}
- 质量退化怎么发现：{{}}

### 11.6 成本与预算

- 单任务成本上限与熔断：{{}}
- 模型路由：{{贵任务 / 便宜任务用什么模型}}

### 11.7 失败与卡死

- 循环 / 卡死检测与兜底：{{}}
- 交回人的条件：{{}}

### 11.8 会话与状态

- 中断恢复 resume：{{}}
- 历史 / transcript：{{留不留、给谁看}}

---

## 写作要点

1. **AI 使用说明 / 完成定义**：这两节是给 AI 看的固定指令，按需微调，让 AI 判断范围和完成。
2. **产品上下文**：锁定一类具体主用户 + 一个有时间地点的真实问题实例 + 量化代价；核心价值一句话；成功标准要能数出来，不用注册量这类虚荣指标。
3. **范围**：本版本做的和不做的都给编号、标 P0/P1；至少 3 条显式非目标，说不出说明范围没想清。
4. **用户任务 TASK**：用户要达成的目标，独立于功能怎么实现；一个 TASK 可对应多个 REQ。
5. **用户流程 FLOW**：关联 TASK，主路径分步、标出分支和边界情况、写清完成状态。
6. **功能需求 REQ**：关联 TASK / FLOW；规则用 MUST / SHOULD；输入逐字段写类型和校验；状态五态逐一，空态不等于错误态；验收写成 Given-When-Then，Then 给具体可观察结果，模糊词换数字。
7. **AI 能力规格**：每个 AI 功能补质量条、触发方式（建议 / 自动）、不确定时行为、服务降级，单列护栏写明绝不能做什么。AI 是概率性的，不钉死就是赌。
8. **数据模型**：核心实体 + 关系 + 数据规则（创建 / 更新 / 删除 / 权限）；纯前端产品可略。
9. **外部依赖 / 非功能**：依赖标是否必需；非功能 6 类逐项给数字或断点、标 P0 / P1。
10. **假设与待确认**：信息不全时能推断的写假设（带依据和错误风险），不能判断的写待确认，别自行扩展需求。
11. **Agent 系统规格**：仅当产品本身是 agent / 自主系统时填，覆盖自主性 / 工具 / 上下文 / 编排 / eval / 成本 / 失败 / 状态。
12. **跨文档一致**：编号 ID（TASK / FLOW / REQ）供 Design Brief 引用，两边 P0 内容保持一致。
