# 产品需求文档：恒鑫智图

## 2026-09-23 · 三项生产整改授权

用户授权实施：修复可恢复重连导致的最终成品误判；阻止 needrestart 自动重启专用 CLI Worker，保留系统安全更新，以停止消费、排空在途任务后受控重启；API 全站同时5任务，超额排队。完成隔离测试与独立审查后切换生产。已确认误判仅在进程退出、当前轮次未变且最终成品校验通过后补收，保留原始证据，不重新生图。性能整改及恢复轮自动干预扩展暂缓。

## 2026-09-23 · CLI 最终交付判定（已修复上线）

中途可恢复重连不应否决最终成功：同一会话正常完成、进程成功退出、最终明确交付且成品通过校验时，必须正常收图入库。真正终止错误、会话错配、无效或缺失成品仍判失败，不得仅凭模型文字或候选文件存在放行。已完成修复上线，三笔误判各4张成品已验证补收，原始诊断证据保留。证据、涉及代码及回归标准见 [缺陷记录](hengxin-smart-image/docs/CLI-FINAL-DELIVERY-FALSE-FAILURE-20260923.md)。

## 2026-09-23 · API 换套图五任务并发

API 换套图全站最多同时执行5个任务，按任务而非账号计数，第6个及以后排队；空出名额后按原任务创建顺序准入。已准入任务在批次切换、退避和收图期间保留名额，整套终态后释放。已完成任务再次修改/重试重新排队，不能挤占在途任务；同任务在途追加修改不另占名额。
每任务仍最多10图并行，严格按10张分批；全站最多50张在途。API Worker部署池同步为5。重复派发和多进程竞争受数据库通道行锁约束。暂停与不确定调用的保守阻断、已返回结果安全收图、原重试及版本逻辑保留。Codex CLI调度不变。本轮按三项整改授权完成实现、隔离测试、独立审查及生产部署。


## 2026-09-23 · CLI 优化生产发布授权

用户明确授权打包最新代码部署生产，再提交Git。发布已审CLI后端改动，包含首轮失败自动干预，以及已在线的主/详情图提示词区分、单张成品修改；生产CLI维持统一0.156.1。仅在专用Worker空闲时备份切换，验收文件哈希、运行心跳、CLI版本和已安装代码的干预测试。不新增收费生图任务，不执行数据库迁移。提交使用中文信息，未要求推送远端。

## 2026-09-23 · CLI 首轮换套图自动干预

新任务首次整套 CLI 换图（壁纸/商品模板任务）在进程明确退出后，如执行失败或最终成品交付校验失败，自动续接原 session_id 一次，沿用同一工作目录、素材、候选、进度及该业务轮次开始时的收图基线。不依赖模型失败措辞识别；通用干预要求先复核是否过度否决，再实际修复明确缺陷，保留已合格结果，最终按原顺序交齐本批成品，不降低用户要求。

上限为首次调用之外1次，与首次调用共用冻结的执行总时限；成功即正常发布，再次失败终止并保留日志和进度。单张修改、人工整套返工、手动重试及历史失败任务不自动补跑。取消/删除、租约失效或进程状态不明、会话丢失/不一致、明确认证/额度/限流错误不续跑；准备材料或保存发布结果失败不重新生成。继续校验文件安全、完整性、格式和数量。

干预前持久记录已消费次数，保留两次独立执行证据并汇总已知用量；切换进程身份时不能让恢复器误收首轮旧结果。Worker 中断后只核实/收取已执行结果，不自动重放干预。页面现有执行时间线展示“自动继续处理（1/1）”。本次授权为代码实现与验证，生产部署须另行明确确认。

## 2026-09-23 · CLI 单张成品修改简化

单张成品修改在原 CLI 会话续接，使用选定的无标注成品作为唯一修改基础，壁纸模式提供原手机屏幕素材，可附一张问题截图/标注图及审批意见。提示词依次为成品、参考素材、可选问题图（标注不进入成品）、可选修改意见、仅交付修改后的1张且其他内容保持不变。该轮不调用或挂载首轮 Skill、不传原底图、不重复首轮换图要求；原会话历史保留。主图、详情图统一该规则。商品/文字模式沿用各自素材含义，不误标手机素材。没有成品的失败图重试仍走原生成流程；首次生成和整套返工保持原逻辑。本条覆盖此前单张修改携带原模板及 Skill 的要求。

## 2026-09-23 · 详情图镜头范围说明

仅 `jd-detail-screen-swap`、`jd-detail-screen-swap-790x1500` 在壁纸模式下使用“把这张手机屏幕素材替换掉图片的壁纸与镜头，镜头指屏内前置镜头或开孔位置，其他一律不允许有任何改变。”，覆盖首次生成和返工，多素材沿用“这些”。两个主图 Skill `jd-main-image-wallpaper-camera-swap`、`jd-main-image-wallpaper-camera-swap-it-optimized` 保持原句；其他 Skill、商品/文字模式、Skill 正文与绑定不变。本条细化下文通用壁纸要求。

## 2026-09-22 · API图片处理动效

API换套图复用CLI图片占位的蓝色光晕流动与图标呼吸动效。生成、收图时动画；排队、退避等待、失败、待核实时不播放处理动效，文字区分真实状态。修改已有图片保留旧结果和预览入口，以局部状态层提示进度；成功后显示当前新版本。保留离屏/后台暂停和减少动态效果偏好支持，不刷新整页。


## 2026-09-22 · API 换套图正式优化开发

本机正式实现已完成：Linux后端924项通过（64项条件跳过）、前端153项通过、类型/构建/真实HTTP隔离联调及独立两阶段审查通过。已部署生产（api-opt-20260922-27d41a2），详见 [验证记录](docs/API-IMAGE-OPTIMIZATION-IMPLEMENTATION-VALIDATION.md)。

用户确认按六项范围开始正式开发，承接已审前端预览。数据迁移、接口与Worker、真实前端及验证以 [正式实现计划](docs/API-IMAGE-OPTIMIZATION-IMPLEMENTATION.md) 为准。本轮包含10张分批并行、失败重试、页面整改、ZIP、单图修改及图片历史版本。


## 2026-09-22 · 单图历史版本预览

本地预览新增图片版本保存、历史对比、下载旧版与设为当前；当前版参与后续修改和 ZIP。沿用现有蓝色主题、卡片和弹框。实施与验收按 [本轮计划](docs/API-IMAGE-VERSION-PREVIEW.md)，不涉及生产部署。

## 2026-09-22 · API 优化需求确认与前端预览

用户确认每批最多10图并行（11张按10+1分两轮）；失败单图首调之外最多3次指数退避，耗尽保留成功项并支持手动继续重试；记录页紧凑缩略图、静默更新与操作人；整套成功ZIP下载；成功图可提交单图修改，以当前生成结果+可选1张标注图+文字意见为输入，图文至少一种，不内置业务提示词，失败保留旧结果，修改遵循同样重试规则。当前仅授权基于现有前端一比一的本地交互预览，细化标准及实施计划见 [预览范围](docs/API-IMAGE-OPTIMIZATION-PREVIEW.md)。正式业务和上线尚未在本轮实现。

## 2026-09-22 · API 换套图正式开发确认

用户在审阅隔离方案与交互预览后授权正式开发，目的是独立验证CLI与API两种方案。真实接口、任务/图片/请求记录、文件关联、队列、Worker、配置全部业务隔离，共用登录与基础设施；不切换、不互相兜底。此次开发以 [独立 API 实施计划](docs/API-IMAGE-IMPLEMENTATION.md) 的规则、契约、步骤和验收为准，覆盖真实前后端、串行、三次指数退避、结果持久化、失败项恢复、不确定执行核实及比较指标。先前“仅Demo”限于历史预览阶段，本轮不自动部署或推送。

## 2026-09-22 图片处理入口的新任务语义

每次点击图片处理菜单的替换壁纸、替换商品、替换文字，进入独立的新任务表单，清空上一次的素材、任务信息与已受理回执；同页重复点击也开始新表单。历史及运行中任务统一通过任务中心查看，不因前一个任务未完成而占用创建入口。显式新建不取消旧任务；原提交会话的未确认请求仍保留原幂等键，不自动重放或改写成新的提交。

## 2026-09-22 · API 换套图独立模块与交互预览

用户已确认新增独立于 Codex CLI 的 API 换套图能力：前 N 张原图分别搭配末张共用素材，使用同一份系统提交的提示词，串行请求 N 次并按原图顺序汇总。中转站 `/v1/images/edits`；固定 model=`gpt-image-2.5-sunburst`、size=`1024x1024`、resolution=`1K`、quality=`xhigh`、n=1。失败指数退避重试三次（首调之外最多三次）；后端实际错误分类与不确定结果恢复待正式实现计划细化。凭据仅服务端保管，不进入本文件。

本次用户要求先基于当前前端查看交互预览。范围为独立 Demo 菜单、新建上传排序与提示词、换图记录、进度/重试/部分失败及示例结果预览下载，详细验收见 [API 交互预览计划](docs/API-IMAGE-PREVIEW-PLAN.md)。全部沿用当前 Art 布局与 Element Plus 组件，模拟任务与现有 CLI 任务隔离，仅本地 demo 模式可用；刷新重置这一模块的演示记录。生产 API、数据库、Worker、部署均不在本轮交付范围。

## 品牌确认 · 2026-09-14

- 用户已确认采用「恒鑫智图」名称及红色 H 形几何标识、右上橙色菱形的方案，所属公司为「前海恒鑫」，用途副标题为「京东业务生图」。此前的「恒信 AI 换套图」「恒信 · 智能图像」为历史名称。
- 本次交付品牌 SVG、透明 PNG、应用图标及使用说明，并接入正式前端的共享 Logo、登录页、工作区名称及浏览器标题/图标。采用已确认概念稿，允许为矢量化、字重及小尺寸清晰度作细化。
- 本次只调整品牌展示；业务功能、权限、运行配置、现有 Art 组件及蓝色操作主题保持原有要求。历史原型和阶段证据保留当时名称。
- 验收：资源脱离字体环境仍可显示；浅/深背景和 16/24/32px 图标可辨识；登录、展开/折叠侧栏及页面标题显示统一名称；前端类型检查、单测、构建及品牌界面检查通过。

## 0. 文档状态与依据

2026-09-17 执行模型确认：生产换图首次执行与返工续接均显式指定 `gpt-6-astra`、推理强度 `high`，不跟随账号默认模型；不改变 Skill 绑定或提示词，不自动重跑历史失败任务。

最新导航确认（2026-09-08）：一级菜单“图片处理”，下设三个二级菜单“替换壁纸”“替换商品”“替换文字”。各自打开原有独立页面；取消页内三标签方案。此前“两个二级菜单”的表述已由用户纠正，不再待确认。

- 版本：v0.26（Skill 轻量管理及 UI 迭代）；日期：2026-09-22；状态：Phase 1–11A 已按历史范围验收；12.1 已实现并审查通过；12.2 双端授权代码及钉钉容器免登修复已部署，真实双端业务验收未完成；13.1–13.3 管理统计/监控/配置已部署到 VPS 开发测试环境，真实角色联调待验收；14.1 VPS 与 HTTPS 已部署，三类真实 Skill、恢复演练和容量验收未完成。最近已验证的 VPS 发布为 `light-skills-20260921-f02b1e0`，迁移 `0014`，包含所见版本返工及 Skill 轻量管理；两个本地壁纸 Skill 可用。2026-09-22 UI 已部署为 `ui-20260922-c837abf`，文件哈希、健康及匿名登录页通过；登录后业务未在本次线上复测，见 [本轮发布记录](hengxin-smart-image/docs/UI-RELEASE-20260922.md)。真实换图尚未验收，依赖审计未全部通过，详见发布记录。详见 DEV-PLAN.md 与开发交接文档。部署到开发测试环境不等于阶段验收通过。
- 产品范围以用户当前会话确认内容为准；业务交付包作为参考，原始文件不改动。
- 原交付包：`C:/Users/82358/Downloads/设计部-国内生图资料包/`。
- 本文统一记录功能、流程与验收依据；“已确认”表示用户明确要求，“建议默认”不表示用户已批准，“待定”不允许开发自行扩展。
- 用户明确要求：Skill 暂未提供，不阻塞 PRD；图片效果与效果验收本轮暂不讨论。本文仅定义业务行为和平台功能验收，不承诺生成质量、成功率或生成时长。
- 本文不代表服务器已检查、CLI 已安装或容量已验证，不指定尚未调研的框架、模型和 API 版本。

## 1. 产品背景与目标

设计人员和运营人员需要复用已有商品主图、副图等图片，完成壁纸、商品主体或文字替换。系统通过图片处理一级菜单下的三个页面收集素材及要求，由服务器上的 Codex CLI 调用指定 Skill 执行，返回图片供预览、反馈返工、下载和归档。

已确认用户角色为系统超级管理员（项目负责人本人）、设计主管、设计人员、运营人员；初期约 20 人，后期最多约 100 人（已确认）。该人数是使用规模，不等于同时运行 20 或 100 个生成任务。

业务目标是形成“复用模板 → 上传素材 → 生成 → 修改 → 下载 → 归档”的完整流程。原业务资料中的 20–30 SKU/日、30 分钟完成和 100% 无需修图不作为本版承诺。

## 2. 第一期范围

| 编号 | 功能 | 优先级 | 确认状态 |
|---|---|---|---|
| SCOPE-001 | 图片处理内的壁纸模式与对应 Skill | P0 | 已确认 |
| SCOPE-002 | 图片处理内的商品模式与对应 Skill | P0 | 已确认 |
| SCOPE-003 | 图片处理内的文字模式与对应 Skill | P0 | 已确认，不细拆复杂交互 |
| SCOPE-004 | 设计/运营上传套图模板并绑定已发布 Skill，选择模板生成 | P0 | 已确认 |
| SCOPE-005 | 新任务独立 CLI 会话、进度及图片结果回显 | P0 | 已确认 |
| SCOPE-006 | 整套或单张修改意见与重新生成 | P0 | 已确认 |
| SCOPE-007 | 预览下载、独立成品归档库 | P0 | 已确认 |
| SCOPE-008 | 四角色、钉钉双端登录、资源权限与历史保护 | P0 | 四角色、双端登录及全员查看/删除已确认 |
| SCOPE-009 | 管理中心：每日调用统计、执行监控及系统配置 | P0 | 用户要求设计，细则见第 13 节|

本期不主动扩展（前两项已确认，其余为建议范围边界）：

- OUT-001：不把换文字发展为复杂排版编辑器。
- OUT-002：本轮不制作 Skill，不进行图片效果评测。
- OUT-003：生成结果不新增主管审批流，修改意见由使用者提交；模板上传保存后直接可用，不设主管发布审批。
- OUT-004：不直接沿用 PSD 图层编辑、涂抹遮罩和落地点配置工具。
- OUT-005：不包含商品平台自动上架、移动端完整编辑和多 SKU 批次编排；如保留原稿批次功能需另行确认。

## 3. 用户任务与流程

| 任务 | 用户目标 | 对应流程 |
|---|---|---|
| TASK-001 | 沉淀可复用模板及处理规则 | FLOW-001 |
| TASK-002 | 用新素材完成壁纸或商品替换 | FLOW-002 |
| TASK-003 | 对图片进行文字替换 | FLOW-003 |
| TASK-004 | 提交整套或单张修改反馈 | FLOW-004 |
| TASK-005 | 下载并保存满意结果 | FLOW-005 |

### FLOW-001：模板准备

设计/运营进入模板库 → 新建模板 → 上传现有商品图片 → 选择适用功能 → 绑定超级管理员已安装发布的 Skill → 保存后直接可用（无需主管发布） → 所有系统用户可在相应入口选择使用。无可用 Skill 可先存草稿；上传和安装 Skill 不属于此流程。

建议默认：模板允许先保存草稿；未绑定可用 Skill 时提示“暂不可生成”。图片允许排序和移除；模板更新形成新版本，不改变已提交任务。

### FLOW-002：壁纸或商品替换

进入图片处理并选择壁纸或商品类型 → 选择适用模板 → 上传替换素材 → 提交 → 持久保存业务任务并返回受理标识 → 排队 → Worker 领取后创建独立 CLI 会话并执行 → 回显图片结果 → 进入预览与修改。

模板缺失、Skill 未就绪或素材无法读取时，说明具体原因。建议默认：每次提交针对一个商品/一组素材，已确认输出张数跟随模板实际图片数；文字任务跟随输入图片数，不强制 8＋12 张。

### FLOW-003：文字替换

进入替换文字 → 上传待修改图片并提供修改要求 → 异步受理并排队 → Worker 调用对应 Skill → 展示结果 → 可修改、下载、归档。

上传图片加自然语言要求已确认；不强制使用套图模板，不展开文字框选、字体参数等复杂交互。

### FLOW-004：反馈返工

查看结果 → 选择整套或某一张 → 填写修改意见 → 异步提交返工并排队 → Worker 续接原会话 → 展示新结果。

2026-09-21 已确认：单张修改以用户点击“修改这张”时该卡片正在显示的图片版本为基础；显示 V1 就修改 V1，显示 V2 就修改 V2。弹窗显示基础版本，可选上传问题圈注截图并填写文字意见；提交后将选定版本的无标注成品、圈注截图（如有）与意见传入原会话，生成该位置的新版本。

单张返工仅更新目标图片。建议保留历史版本，在同一业务任务内记录修改轮次；已确认首次生成及后续整套、单张修改均续接该任务同一 CLI 会话，同一运营提交新任务则创建新会话。

### FLOW-005：下载与归档

运营预览后可以下载；满意后执行独立归档操作 → 成品库显示归档结果 → 后续查看与下载。

建议默认：单张下载与整套打包并存；归档保存选定结果版本，后续返工不覆盖已归档文件。

## 4. 功能需求

### REQ-001：一级菜单下的三种处理页面（TASK-002/003，FLOW-002/003）

- 侧栏一级菜单为“图片处理”，下设替换壁纸、替换商品、替换文字三个二级菜单，各自进入独立页面并调用相应 Skill。
- 壁纸模块修改屏幕内图案，商品模块修改模板中的商品主体，文字模块按要求修改文字；具体处理方法由 Skill 承载。
- 不能把某类任务误送给另一类 Skill；允许共用任务与成品基础设施。

### REQ-002：模板与 Skill 关联（TASK-001，FLOW-001）

2026-09-21 现行绑定规则：模板与模块默认关联稳定 Skill 身份，新任务解析最近一次同步成功的不可变执行快照；已提交任务及返工保留原快照。下列人工选择/冻结 Skill 版本的旧规则已由第 13 节轻量管理替代，模板图片版本与历史保护继续保留。

- 设计人员和运营人员均可上传已有商品图片建立模板，绑定已发布 Skill。
- 建议字段：模板名称（必填）、适用功能（必填）、有序图片列表（必填）、Skill 引用及版本（执行必需）、备注（可选）。
- 已确认：模板指定专用版本时使用该版本，未指定时使用对应模块默认版本；无可用版本时可存草稿但不能生成。仅系统超级管理员维护 Skill 和模块默认绑定。保存模板时冻结解析出的 Skill 版本和图片顺序，后续默认切换不改旧版本，重新保存才解析新默认。显式绑定失效时保留原绑定并阻止生成，不静默替换。实际包格式按技术方案验证。
- Skill 尚不存在时允许准备模板草稿，但不得将模拟执行显示为真实生成成功。
- 已确认全员可查看、编辑、停用和删除模板；上传保存后无需审批即可使用（生成仍需可用 Skill）。建议支持搜索；被历史任务引用的版本保留。

### REQ-003：素材与提交（TASK-002/003，FLOW-002/003）

- 壁纸和商品任务需要功能类型、模板和替换素材；文字任务需要待修改图片和自然语言修改要求（已确认）。
- 提交前检查文件是否为空、可读取及是否属于支持类型。已确认 JPG/PNG/WebP、每张不超过 10 MiB、每组最多 20 张，模板和输入素材分别计数。
- 已确认任务名称必填，SKU 可选。
- 已确认重复点击、网络重试和同一请求重放不得创建重复任务或轮次；服务端按第 9.2 节保证幂等。已提交任务冻结输入及模板/Skill 版本。

### REQ-004：任务执行与进度（TASK-002/003，FLOW-002/003）

- 已确认全员可查看和删除任务，记录实际删除操作者；删除不破坏仍被使用的历史文件引用。

- 新生成任务通过服务器上的 Codex CLI 创建独立会话，固定调用指定 Skill。
- 系统需要保存业务任务和 CLI 会话的关联，以及输出图片与模板图片的对应关系。
- 已确认一项业务任务独占一个 Codex 会话，不按用户共享；同一任务从受理到执行、取消处理或状态核实期间只允许一个未结束轮次，具体生命周期与并发规则见第 9.1–9.2 节。
- 前端展示真实执行状态；只有获取到可读取的输出图片才可展示为该图片生成完成。
- 已确认采用异步后台队列：首次生成和返工提交在校验并持久保存后返回任务/轮次标识，不等待 CLI 或生成完成；关闭浏览器或退出登录不终止已提交任务，前端独立查询状态和结果。
- 工作区顶部提供明确的“退出登录”入口：真实模式先注销服务端会话，再清理前端身份和页面状态并进入钉钉登录页；注销失败提示重试，不声称已退出。刷新登录页不得自动返回工作区。模拟模式仅退出预览，不冒充真实钉钉认证。
- 钉钉网页授权后以用户令牌获取个人资料，再通过企业应用凭据将 unionId 核验为企业 userId，禁止直接把 openId 当成员 ID 授权；回调失败须显示原因类别，不静默返回正常登录提示。
- 网页授权 state 必须绑定发起请求的浏览器并仅消费一次；认证失败也不能撤销消费记录，不接受其他浏览器携带的回调。
- 建议并发数量可配置，超过运行名额时排队；不得按账户总人数直接启动同等数量进程。
- 任务进度来自执行事件或已完成图片数；没有可靠比例时只显示处理阶段，不编造百分比。

### REQ-005：审阅与修改（TASK-004，FLOW-004）

- 支持整套预览和单张查看，整套与单张均可填写修改意见。
- 返工输入包含指定 Skill、目标图片范围、相关素材与本轮意见。
- 单张返工不得更新其他图片；建议保留旧版本，新结果成功后才更新当前版本。
- 单张返工冻结用户点击时正在查看的结果版本 ID，弹窗明确显示该版本；后端校验版本属于该任务、该图片位置且文件可用，不得静默改用最新版本。此处修改底图是选定版本的无标注成品，不是初始模板图；原模板与素材可继续作为辅助参照。
- 单张修改弹窗增加可选问题截图上传框，首版每轮最多 1 张，沿用 JPG/PNG/WebP、单张不超过 10 MiB 及后台更严格限制；支持预览、移除和替换，上传中或失败未处理时不能提交。未上传仍可按文字意见修改。截图仅帮助定位问题，圈线、箭头、文字标记及截图界面不属于成品内容，不得复制到最终图片；无法确定对应位置时按真实歧义处理，不猜测修改区域。
- 本轮意见、基础版本及问题截图共同保存为返工记录；重试沿用同一输入快照。同会话历史中的其他版本或截图不得替代本轮明确指定的输入。
- 从历史版本返工成功后追加新版本并设为该位置当前版本，不覆盖既有文件或复用旧版本号。例如已有 V1/V2，基于 V1 修改成功生成 V3；失败保留原当前版本。整套修改仍基于各位置当前版本，查看历史版本本身不改变整套下载或归档结果。
- 已确认同一任务的首次生成、整套返工、单张返工和重试互斥；不同用户同时操作同一任务也适用。已有轮次未结束时拒绝新的修改请求并保留用户意见，同一已受理请求的重放返回原受理结果。
- 不另设主管审批节点。

### REQ-006：下载与归档（TASK-005，FLOW-005）

- 图片结果可从前端下载，满意结果可归档到独立成品库。
- 已确认全员可查看和删除成品；建议支持名称搜索、功能类型筛选、预览、单张下载与整套打包。
- 建议归档关联任务、结果版本、创建人及归档时间；重复点击同一归档动作不重复创建记录。
- 建议部分失败时允许查看、下载已成功的单张，但不把残缺结果标为完整套图；默认全部完成后才整套归档。
- 建议默认保持源图尺寸与格式，具体格式兼容性、命名和压缩要求待定。

### REQ-007：四角色与管理中心

- 已确认设计人员与运营人员拥有完全相同的功能权限及数据范围规则，均为日常生产使用者；角色名称仅用于岗位分类和统计，不限制设计人员只能测试模板。

- 使用第 7 节四角色及第 13 节中的数据范围；提供用户角色分配、个人/全员调用统计及 Codex 执行监控。
- 系统配置仅超级管理员可修改；设计主管的业务管理权限独立于系统配置权限。调用次数按真实执行 attempt 统计，与任务数和模型内部请求数区分。

### REQ-008：钉钉双端登录

- 同一套现有 Vue 前端支持钉钉电脑端内嵌企业网页应用和电脑浏览器。前者使用钉钉容器免登，后者使用钉钉网页授权/扫码登录；两入口均由后端验证身份并创建系统会话。
- 按目标企业成员身份绑定，不按显示名匹配。首次未分配角色显示待授权，企业外成员、已禁用账号不可进入业务。超级管理员由部署时明确指定的成员身份初始化，禁止首位登录者自动提权。
- 授权失效、拒绝、钉钉不可用有可重试提示；上传、下载、预览、异步提交、返工及归档在两端验证。仅桌面范围，不扩展手机布局。

## 5. 页面与状态

### 路由体验修复确认 · 2026-09-14

用户在路由体验专项审查后要求修复，本次落实以下验收要求：
- 任务详情沿用抽屉，以 `task` query 定位，兼容旧 `taskId`；打开、切换、关闭、删除及浏览器前进后退时地址和对象一致。成品详情沿用弹窗，以 `archive` query 定位，支持刷新及站内链接恢复。
- 任务与成品列表的类型、搜索和页码（任务另含状态）保存在 URL，刷新和浏览器跨页返回恢复上下文；修改筛选重置页码，关闭详情保留筛选。
- 归档成功提供查看该成品入口；成品详情可查看来源任务，来源不可用时说明原因并保留归档图片和下载。
- 任务中心新建入口明确列出壁纸、商品、文字三类；成品筛选无匹配时可清除筛选。
- 创建页准确提示已有返工和归档能力；无权限直达显示无权访问并可返回工作区，不静默跳回首页。
- 不改变一级菜单、抽屉/弹窗先例、业务数据权限及真实钉钉和上线阶段的验收边界。

导航：图片处理、任务中心、模板库、成品库、管理中心。Phase 1–4 已完成前端页面及模拟交互；管理中心按角色展示，后续已接入真实后端授权，完整真实角色联调仍待验收。图片处理一级菜单可展开或收起，包含替换壁纸、替换商品、替换文字三个二级菜单。Skill 管理位于管理中心，仅超级管理员可操作；日常执行只选择已发布版本。

| 页面 | 主要操作 | 空状态 | 加载/处理中 | 错误状态 | 成功状态 |
|---|---|---|---|---|---|
| 壁纸/商品入口 | 选模板、传素材、提交 | 提示选择模板和上传 | 上传进度/提交中 | 指出缺失项或不可用 Skill | 进入任务详情 |
| 文字入口 | 传图、填要求、提交 | 提示上传 | 上传/提交中 | 指出输入问题 | 进入任务详情 |
| 模板库及编辑 | 创建、绑定、预览、维护、全员删除 | 创建首个模板 | 保存中 | 保存失败保留草稿 | 保存反馈 |
| 任务中心 | 筛选、查看全员详情、删除 | 创建任务入口 | 排队及执行阶段 | 失败原因与重试入口 | 待查看结果 |
| 结果详情 | 反馈、下载、归档 | 尚无结果 | 目标图片重生成中 | 保留既有结果并提示 | 新版本可预览 |
| 成品库 | 搜索、预览、下载、全员删除 | 尚无归档成品 | 加载中 | 刷新/下载重试 | 查看归档图片 |
| 钉钉登录 | 容器免登或浏览器授权 | 登录入口 | 授权验证中 | 拒绝、失效、企业不符、待授权 | 进入获授权页面 |
| 调用统计 | 日期/用户/类型筛选、明细 | 暂无执行 | 查询中 | 查询失败/无权限 | 个人或全员汇总 |
| 执行监控 | 队列与环境状态 | 暂无执行节点 | 检查中 | 异常、失联、未知 | 就绪/空闲或执行中 |
| 用户与角色 | 分配角色、启停 | 暂无成员记录 | 保存中 | 校验失败/无权限 | 授权结果生效 |
| Skill 管理 | 同步、启停、设默认、移除登记 | 暂无 Skill | 同步中 | 异常原因及重试 | Skill 可用 |
| 系统配置 | 允许参数编辑及审计 | 未配置提示 | 保存中 | 越界/无权限 | 后续轮次使用新配置 |

建议统一无权限状态：不返回受限图片和资源，显示无权访问；登录过期后重新登录可回到原任务。

建议任务状态：排队中 → 执行中 → 待查看；执行异常进入失败/部分失败；重新生成进入执行中；归档作为结果记录，不代替任务执行状态。删除触发取消、执行状态待核实及恢复规则已按第 9.2 节确认；具体超时和重试参数仍按第 11 节处理。

## 6. 数据模型与历史

| 实体 | 关键内容 | 关系 |
|---|---|---|
| 用户 | 标识、名称、角色、状态 | 创建模板、任务和归档 |
| 模板及版本 | 名称、类型、图片顺序、Skill 绑定 | 一个模板具有多个版本 |
| Skill 及版本 | 类型、包或引用、可用状态 | 关联模块、模板和任务 |
| 素材 | 文件、类型、尺寸、所属任务 | 一个任务关联多个素材 |
| 生成任务 | 类型、创建人、状态、输入快照、会话标识 | 关联执行轮次 |
| 执行轮次 | 修改目标、意见、起止时间、失败原因 | 产出图片版本 |
| 图片版本 | 原模板图标识、文件、轮次、当前版本 | 单张返工独立追踪 |
| 归档记录 | 任务、选定版本、文件、归档人及时间 | 保存满意结果 |
| 钉钉身份与角色 | 企业成员标识、角色、账号状态 | 两端绑定同一用户 |
| 执行尝试与 usage | 实际操作者、轮次、开始结束、结果、可空用量 | 一轮可关联多次真实尝试，统计按尝试去重 |
| Worker 状态 | 节点、心跳、检查时间、依赖状态 | 监控执行可用性 |
| 系统配置与审计 | 参数版本、操作者、变更前后值 | 后续轮次读取并冻结配置 |

这些字段是建议实现模型；数据库已确定 PostgreSQL，文件存储已确定 MinIO，具体表结构按开发计划实现。任务与素材须按任务隔离；不同运营的执行不能读错输入或覆盖他人输出。

## 7. 角色、登录与生命周期

- 已确认四角色：系统超级管理员、设计主管、设计人员、运营人员。具体权限矩阵见第 13 节；此前管理员/运营两角色建议由此替代。
- 已确认钉钉电脑端企业内部应用及电脑 Web 均支持钉钉登录；同一钉钉成员映射同一系统账号，角色由超级管理员分配，不从钉钉职称自动推断。正式方案不再默认使用本地用户名密码登录。
- 设计与运营均可创建维护模板且权限完全一致；模板上传保存后直接可用，无需主管发布。生成仍需绑定已安装可用 Skill。
- 所有已登录、已授权且启用的系统用户均可查看和删除所有人的模板、任务及成品，不按创建人或团队限制这两类操作；设计主管可查看全员任务和调用统计。
- 未提交素材和草稿可移除；可用模板可停用，删除不破坏历史任务引用。
- 模板、任务和成品均提供全员可用的删除入口，记录实际操作者；回收站形式及保留期限仍为建议，删除不破坏历史引用。
- 清理任务时不得连带删除仍被归档引用的文件。
- CLI 执行身份、凭据存放及可访问目录由技术设计确定；凭据不返回浏览器。

## 8. 部署与非功能要求

### 8.1 已确认的部署环境（2026-09-09 已实机检查）

| 项目 | 内容 |
|---|---|
| Codex CLI 部署位置 | Linux 服务器 |
| 主机名称 | racknerd-058889d |
| 发行版本 | Ubuntu 24.04 LTS |
| 内核 | 6.8.0-138-generic |
| 架构 | x86_64 |
| 初期使用人数 | 约 20 人 |
| 后期使用人数上限 | 约 100 人 |

实机为7个逻辑CPU、7.8GiB内存、根盘剩余约80GiB；检查时可用内存约2GiB，带宽未测。当前开发计划采用单机部署基线，实际容量须在部署前验证。2026-09-10确认使用codex专用执行身份、生成并发1、单轮60分钟硬超时、失败不自动重跑。

### 8.2 非功能需求

| 类别 | 需求/建议目标 | 状态 |
|---|---|---|
| 规模 | 面向初期 20 人、后期约 100 人使用 | 已确认 |
| 并发 | 在线并发、执行并发、峰值提交量分别记录，执行名额可配置 | 建议；具体数值待定 |
| 可靠性 | 后台持久保存任务；服务重启可识别未完成任务，避免重复生成或错误标成功 | 建议 P0 |
| 执行并发安全 | 任务独立会话、同任务互斥、重复消息单次认领、取消及失效执行不能覆盖结果，见第 9.2 节 | 已确认 P0 |
| 故障处理 | 保留成功图片及失败原因；允许人工重试，自动次数与超时待定 | 建议 P0 |
| 数据保护 | 登录鉴权、任务隔离、文件访问校验，归档不被后续返工覆盖 | 建议 P0 |
| 兼容性 | 钉钉电脑端内部应用与电脑浏览器共用前端，均支持钉钉登录；不包含手机布局 | 已确认 P0 |
| 可访问性 | 输入有标签，错误有文字，状态不只靠颜色 | 建议 P1 |
| 性能 | 页面响应、上传上限、队列等待与生成时长阈值待开发计划明确 | 待定 |

### 8.3 开发机 Codex CLI 真实联调（2026-09-11 新增）

用户要求增加独立开发阶段，在开发机接入 Codex CLI，使用实际上传的 Skill、模板和素材进行测试，安排在 Phase 11 后、Phase 12 钉钉接入前；编号为 Phase 11A。该要求不代表当前本机测试执行器已经调用真实 AI。

- 开发机浏览器 → 本机 API/PG/MinIO/Redis → 开发机可用执行环境中的 Worker → Codex CLI，完成真实生成、结果预览、同会话整套/单张返工及下载归档。首先以用户已上传的壁纸 Skill 跑通；商品、文字 Skill 在提供并通过依赖检查后纳入对应测试，不把测试包当真实业务包。
- 接入前检查本机 CLI 路径与版本、认证可用性、图像工具、Skill 工具依赖及隔离能力。现有实现要求 Linux 与 Bubblewrap；Windows 开发机须验证本机 WSL/Linux 执行环境可否复用。不能直接指向 Windows CLI 就宣称兼容；若需 Windows 原生适配，先明确实现与安全验收范围，保持同等任务隔离要求。
- 测试环境使用独立数据及任务私有会话目录，保留当前 fixture 任务历史；新建真实任务标识为 cli，不将旧 fixture 任务改标为真实生成。凭据不进入仓库、日志或前端，真实执行与 fixture 消费者不能混用同一生成队列。
- 先验证串行真实业务闭环，再以并发上限 2 和足够 Worker 执行槽验证两个任务同时执行、不同会话/目录、结果不串图；并行参数仅用于隔离的开发测试，默认并发 1、60 分钟超时、失败不自动重跑不变。记录时间重叠、进程身份、资源占用及异常恢复，不将测试并发当作生产容量承诺。
- 验收必须具有真实 CLI attempt、session ID、新生成且可读取的图片、返工版本及下载归档证据；fixture、输入图片复制、单纯退出码成功均不能替代。无实际认证、工具依赖或可用隔离环境时记录具体阻塞，阶段保持未通过。
- 本阶段验证业务链路与执行管理，图像美观评分、其余未提供 Skill、钉钉双端、生产部署和 20/100 用户容量验收仍保留原有范围边界。
- 2026-09-11 用户确认跳过本次尺寸问题：Phase 11A 暂不要求原生输出像素尺寸与模板一致，后续专用联调任务明确按原生输出尺寸验证，不通过缩放伪造一致性；该项作为已知限制记录，不阻塞其他联调。既有失败任务保持原状态，真实生成、会话、版本、下载归档及隔离证据要求不变。本次不修改上传的 Skill 或生产验收规则。

## 9. Agent 与外部依赖

### 生图过程可观测补充（2026-09-11）

- 任务详情默认展示本次提交的替换素材缩略图、名称和数量，支持原图预览；读取任务冻结的sources，不用输出图或当前模板替代。无素材明确提示，图片加载失败可辨认。整套/单图返工继续使用原提交素材。
- 任务详情展示提交时使用的模板名称与版本，优先读取任务冻结的 templateSnapshot，兼容任务自带的 template/templateVersion；不查询模板库最新版。历史记录缺失版本显示“版本未记录”，文字任务未绑定模板时显示“未使用模板”。模板后来更新、改名或删除不改变这里的历史信息。
- 任务详情同时展示本次任务实际使用的 Skill 信息：冻结的 Skill 名称、版本、版本 ID 与内容校验和；优先读取任务冻结的 skillSnapshot，快照缺字段时仅按任务已绑定的 Skill 版本 ID 回查同一历史版本记录，不查询 Skill 管理页当前默认版本。历史任务缺失对应信息时逐项显示“未记录”，不得用当前可用 Skill 冒充历史绑定。
- 任务绑定卡片默认突出模板与 Skill 的业务名称、版本和当前绑定关系；版本 ID、内容校验和归入默认收起的“技术详情”，技术字段支持复制，长值提供可辨认的缩略展示并可查看完整值。模板版本与 Skill 版本统一使用带 `v` 的版本格式。
- 选择性展示Codex面向用户的agent_message执行播报（如查看素材、逐图处理、校验和结果说明），纯文本、限长并过滤代码块、链接目标、本地路径、凭证及控制字符；reasoning、命令原文、原始工具结果不对外展示。播报明确归属Codex，不替代平台验收状态，不能据其文本直接标成功。
- 播报完整保留本轮实际绑定的 Skill 标识，包括普通文本、`名称` 和 `$名称` 写法；以已准备的任务 Skill 标识精确匹配，不因名称超过32字符而隐藏。其他长标识、路径和凭证仍过滤；不从模型任意声明推导白名单。已存储的历史脱敏播报不凭绑定名称猜测回填。
- Codex播报在过程卡片默认显示最近几条，可展开全部保留记录；系统事件另折叠为简洁时间线，合并连续重复内容。历史任务可从原执行JSON事件安全补录agent_message，但不得伪造时间，缺失时间明确标记。补录不改变原状态、结果、绑定或已有系统事件。

- 任务详情展示排队、准备素材、启动执行器、模型处理、校验结果、存储、发布及终态的真实过程时间线；显示耗时、最后活动时间、检测到的图片数量。没有供应方进度时不估算百分比，检测到图片不等于通过校验。
- 长时间无新事件显示“暂未收到新进展”，不能直接认定失败。页面刷新、关闭重开与切换任务后，按任务及轮次读取持久化记录；终态停止轮询，未知状态保留原恢复门禁。
- 失败说明区分启动/认证/网络/超时、Skill报告失败、输出清单/图片校验、存储、执行权失效及未知异常，展示原因、失败阶段、逐图错误和建议操作；建议不自动启动重试或改变已有操作资格。
- 过程事件和错误只使用可公开的结构化摘要。不得暴露模型思维链、原始命令、提示词、凭据、完整stderr或本机路径。每轮提供诊断编号关联内部task/round/attempt记录。
- 历史缺失过程事件明确标注；可以依据保留的真实执行证据补充错误诊断，不伪造历史进度、不改原成功/失败状态。本阶段尺寸豁免不抹除实际Skill报告的失败。
- 第一版复用详情轮询及现有组件，不引入额外监控集群；详细字段与实现任务见 hengxin-smart-image/docs/EXECUTION-OBSERVABILITY.md。

| 编号 | 依赖 | 当前情况 |
|---|---|---|
| DEP-001 | Linux 上的 Codex CLI 执行环境和认证 | 2026-09-23 用户要求生产服务器入口与项目执行器统一为官方最新稳定版0.156.1；保留版本校验、任务隔离和ChatGPT认证。升级验证见 hengxin-smart-image/docs/CODEX-UPGRADE-20260923.md；0.153.4为原验收版本。 |
| DEP-002 | 壁纸、商品、文字三个 Skill | 服务器有壁纸Skill且已单页冒烟；正式平台版本安装及其他两类待联调 |
| DEP-003 | Skill 需要的图像处理工具或服务 | 待 Skill 方案明确，不预先承诺能力 |
| DEP-004 | 文件持久存储与可下载输出 | 已确定 MinIO，部署与访问权限需联调 |
| DEP-005 | 钉钉企业内部应用、授权及成员接口 | 双端登录已确定，企业配置与域名尚未联调 |

- 系统异步执行用户提交的生成和修改任务；设计与运营均可审阅、返工和归档。
- 每个新任务拥有独立 CLI 会话；任务上下文包含指定 Skill、相关图片和要求。
- 已确认按业务任务关联会话，不按运营账号共用：首次生成新建会话，返工按已保存会话 ID 续接；后台每轮启动 CLI 进程，完成后退出，保留会话记录。归档由后端完成，不调用 CLI。
- 第一期不实现自动上下文摘要、历史要求整理或自动切换会话。续接失败明确报错、保留旧结果，不自动新建会话冒充续接。仍保留必要的输入、结果版本和会话 ID；工具权限、进程超时、自动重试次数及费用上限留待技术设计。
- 业务任务状态独立于 CLI 进程存活状态；进程退出或文本回复不能单独作为图片完成证据。
- 日志建议记录任务、执行轮次、错误与输出路径，供定位问题；运营页面显示业务进度及结果。
- 效果评测按用户要求推迟，本轮不设效果样例收集门槛。

### 9.1 任务、会话与进程生命周期（已确认）

“业务任务”是一项套图工作的完整记录，“轮次”是首次生成或一次返工，“Codex 会话”保存这项任务的执行上下文，“CLI 进程”只在具体轮次执行时运行。会话不等于常驻操作系统线程。

| 场景 | 会话与进程规则 |
|---|---|
| 受理新任务 A | 先持久入队；实际执行前可以尚无会话 ID，不能为排队任务伪造 ID 或提前启动 CLI |
| 首次执行 A | 创建会话 A 并保存明确关联；该会话不分配给其他业务任务 |
| A 的本轮执行结束 | CLI 进程退出，保留会话 ID、必要会话材料及输入/结果记录；查看结果、填写意见期间不挂起 CLI 等待用户 |
| A 的单张或整套返工 | 新轮次启动新的 CLI 进程，按已保存的会话 A 续接；结束后进程再次退出 |
| 同一用户或其他用户创建 B | B 实际执行时创建会话 B；会话材料、输入/输出及临时文件与 A 隔离，执行环境不得跨任务读取或修改，不能按账号复用会话或使用共享“最近会话” |
| 归档 A | 后端保存图片版本快照，不调用 CLI，也不因归档自动销毁仍可返工的 A 会话 |
| 删除与清理 A | 先按第 9.2 节失效执行与结果发布权限；会话清理不得影响仍可返工任务、运行进程或归档引用。具体保留天数按第 11 节确认，不在本次指定 |

Worker/容器重建后，仍需能用受保护的持久会话材料按原 ID 续接。会话材料确实缺失或不可恢复时明确报错并保留旧结果，不自动新建会话冒充续接；本期不增加自动上下文重建。

### 9.2 执行并发安全（已确认 P0）

1. **按任务隔离和互斥**：不同任务分别绑定会话，执行环境不得读取或修改其他任务的会话材料、输入、输出及临时文件；同任务从轮次受理入队、执行、收集结果直到最终提交或取消处理结束，最多一个未结束轮次。状态待核实也占用该任务的执行权；不同操作者、不同浏览器与不同 Worker 都不能绕过。整体执行并发上限与此独立，具体数量仍待定。
   执行环境的任务隔离不改变第 13 节的全员业务资源查看权限；具体隔离技术仍需按开发计划设计和实测，不能仅靠不同目录名宣称隔离完成。
2. **重复请求不重复创建**：同一操作者对同一操作提交相同幂等键和相同内容，返回原任务/轮次受理结果；同键异内容拒绝。不同新请求竞争同一忙碌任务时只受理一个，其余明确提示冲突、保留意见，不悄悄追加待执行返工；先完成身份校验，再判断已受理请求重放与新请求冲突。
3. **重复消息不重复启动**：任务执行权和轮次认领在数据库中原子完成，对多个进程和 Worker 生效。业务数据库锁只用于短事务，不能持有锁等待 CLI；仅前端按钮锁、Python 线程锁或队列 task ID 不足以作为保障。成功、已确认失败及已完成取消的轮次不因重投再次执行。
4. **失效执行不得发布结果**：提交图片版本与切换当前版本时，必须再次确认任务未删除、轮次仍有效且提交者持有当前执行权；旧轮次、旧执行者及取消后的迟到结果均不得改变当前图片或复活任务。执行审计和已发生的用量仍可如实记录，旧归档不被破坏。
5. **不确定执行先核实**：Worker 失联、租约过期或连接中断，不能直接证明旧 CLI 已退出或外部调用未发生。结果待核实时不自动再次启动，也不开放同任务返工/重试；先核实旧进程和已产出文件。确认可安全重新执行后，才按已有重试规则受理新轮次并记录实际执行，不能把消息补发计为新的 AI 调用。
6. **删除与取消持久化**：排队任务删除后不能启动 CLI；运行任务删除后记录取消意图、停止本轮进程并核实退出，结果提交仍受第 4 条保护。重启后取消意图继续生效；停止本地进程不能保证撤回上游已受理调用。关闭页面或退出登录不触发取消。

## 10. 平台功能验收标准

以下已确认需求可作为验收基础；涉及建议默认的条目在对应功能实施前按第 11 节处理，不包含图片视觉效果评分。

- [ ] AC-001：选择三种不同处理类型提交时，任务类型和调用的 Skill 与选定类型一致。
- [ ] AC-002：设计或运营上传保存模板并绑定可用 Skill 后，全员可直接从对应入口选择使用，不存在主管发布审批。
- [ ] AC-003：缺失必需素材或可用 Skill 时，提示原因且不伪造生成成功。
- [ ] AC-004：分别由同一用户和不同用户创建两个新任务，实际执行时各自关联不同 CLI 会话，执行环境不能读取或修改另一任务的会话材料、输入、输出及临时文件；后续返工只能续接所属任务会话。
- [ ] AC-005：执行返回可读取图片后，前端可以预览并下载对应文件。
- [ ] AC-006：对整套提交修改意见时，记录意见、目标范围和返回的新结果。
- [ ] AC-007：仅对第 N 张返工时，其他图片的当前版本及文件内容保持不变。
- [ ] AC-007A：同一图片存在 V1/V2 时，分别查看 V1、V2 后打开单张修改，弹窗、轮次记录和执行材料均指向所选版本；基于 V1 成功返工生成 V3，V1/V2 保留，其他位置不变。
- [ ] AC-007B：单张修改可携带圈注截图与文字意见，模型收到选定版本的无标注成品和本轮截图，截图标记不成为成品元素；截图可预览、移除、替换，无截图仍支持文字修改，失败重试保留同一基础版本及截图。
- [ ] AC-008：归档后，在成品库可找到记录并下载归档图片。
- [ ] AC-009：已确认——关闭页面再进入任务中心，能够查看后台任务最新状态。
- [ ] AC-010：已确认——双击、网络重试或并发重放同一次操作只创建一个任务/轮次和对应待发消息，返回相同受理标识；同键异内容拒绝。同任务不同新请求按 AC-021 处理；整体并发数量仍待定，达到已配置上限后不同任务排队。
- [ ] AC-011：建议默认——单张返工失败保留原版本，其他成功图片仍可查看。
- [ ] AC-012：建议默认——模板或 Skill 更新后，已提交任务保留原版本引用。
- [ ] AC-013：建议默认——后续返工不改变之前已归档的文件。
- [ ] AC-014：建议默认——无权限用户无法通过直接文件地址访问受限资源。

- [ ] AC-015：同一任务首次生成、单张返工、整套返工使用相同 CLI 会话 ID；每轮进程结束后退出并保留会话材料，等待人工查看期间没有该任务的 CLI 进程；同一用户第二项新任务使用不同会话 ID，归档不启动 CLI。
- [ ] AC-016：首次生成与返工作业尚未完成时，提交已返回受理标识，状态可独立查询；关闭页面后执行继续。

- [ ] AC-017：四角色均能跨创建人查看、删除模板/任务/成品，设计主管可查看全员任务与统计，其他管理权限符合矩阵；在资源归属等条件相同时，设计与运营的允许/拒绝结果完全一致，角色变更或禁用后旧会话失去相应权限。
- [ ] AC-018：调用明细可复算每日统计，缺失 usage 不显示为 0；Worker 失联与 CLI 空闲正确区分，配置修改可审计。
- [ ] AC-019：同一成员通过钉钉桌面端及浏览器登录映射同一系统用户及角色；陌生企业、未授权/禁用成员不能进入业务。
- [ ] AC-020：两端登录过期、拒绝授权、回调刷新/重放、下载上传和异步任务闭环通过实测。

- [ ] AC-021：两个用户同时对同一任务提交整套与单张返工，只受理一个新轮次；另一请求返回冲突并保留意见。已排队、执行中、收集结果、取消处理中或状态待核实的轮次均阻止新的返工，实际同时运行的 CLI 进程最多一个。
- [ ] AC-022：两个独立 Worker/进程并发收到同一轮次消息，只允许一个启动执行器；最终状态再次收到消息不重跑。长任务执行期间数据库状态仍可及时查询，取消意图可写入，不能以全局执行并发设为 1 代替此验证。
- [ ] AC-023：分别在排队、CLI 执行中、图片已上传但版本尚未提交时删除任务；验证不再启动或发布结果，已取消轮次停止重派，重启后不复活，旧归档不变。人为让失效执行者或旧轮次晚返回，也不能覆盖当前图片。
- [ ] AC-024：首次生成后销毁并重建 Worker 容器，从新的轮次工作目录按原会话 ID 返工成功；任务 A/B 的会话材料、输入、输出及临时文件不能被另一任务的执行环境读取或修改。会话材料缺失时明确失败，不能新建会话冒充恢复或清掉旧图。
- [ ] AC-025：在外部执行已发生但结果未入库时模拟 Worker 中断；重投、租约过期及用户重试都不能在旧进程/结果未核实前启动第二份执行。能恢复的结果继续收集，不能确认时显示状态待核实并禁止返工/重试；核实后才按重试规则处理。
完成定义：P0 范围实现并通过相关平台功能验收，异常和空状态可用，建议默认项完成确认，设计和开发计划已对齐；真实生成联通依赖 Skill 与执行环境就绪。本文完成不等于系统完成或通过容量验收。

## 11. 尚需明确的事项

不阻塞本版 PRD 编写；按依赖阶段处理：

| 编号 | 事项 | 何时需要 |
|---|---|---|
| Q-001 | 格式/10 MiB/20 张及输出张数已确认；真实输出尺寸、格式转换兼容仍待联调 | 真实输出联调前 |
| Q-002 | 模块默认 Skill 与模板专用 Skill 的绑定方式（维护权限已确定仅超级管理员） | 配置功能开发前 |
| Q-003 | 已确认采用上传图片加自然语言要求，任务名称必填、SKU 可选 | 已确认 |
| Q-004 | 回收站形式与保留期限；主管全员任务/统计、模板免审批、全员查看/删除、编辑模板及返工/归档全员任务均已确认 | 账号与资源管理开发前 |
| Q-008 | 企业/App、HTTPS 回调和两名管理员 userId 已配置；用户已确认张帅可登录，完整双端业务实测仍待验收 | 双端真实登录验收前 |
| Q-005 | CPU/内存/磁盘/带宽，执行并发和峰值任务量 | 容量设计与部署前 |
| Q-006 | CLI 认证、实际处理依赖、会话续接/容器重建的实机验证、会话保留清理期限、超时重试及成本上限；并发安全规则已按第 9.2 节确认 | 真实任务联调前 |
| Q-007 | 下载、归档及历史版本的建议默认规则 | 相应功能开发前 |

2026-09-09 用户已确认：JPG/PNG/WebP，单文件 10 MiB、每组最多 20 张；任务名必填、SKU 可选；文字输入采用图片加自然语言；按实际模板/输入张数输出。

其余可审阅默认值仍未确认：新版本保留、完整套图才整套归档；逻辑删除后 30 天回收且引用保护。运行参数已确认：codex 专用身份、初始并发 1、单轮硬超时 60 分钟、自动重跑 0。普通列表 P95 ≤ 1 秒（不含上传/下载/生成），每日备份、RPO 24 小时、RTO 4 小时，仍需联调和资源验证。是否采用其余业务默认在对应阶段确认，设计与运营始终适用相同规则。

图片效果及 Skill 编写均按用户要求后置，不作为当前文档出稿条件。

## 12. 与业务交付包的关系及下游影响

2026-09-08 开发计划确认：用户要求拆分 DEV-PLAN.md；正式前端直接复用当时已认可的页面、组件和导航。后端明确采用 Python FastAPI，数据库 PostgreSQL，图片存储 MinIO。当时授权为规划和本机 Docker 只读检查；之后已进入正式开发，当前进度见本文版本状态与 DEV-PLAN.md，不能将这段历史说明理解为后端基础尚未开发。前端已完成源码承接，旧原型目录随后清理，不能以打包后的 HTML/JS 为开发源。

本机检查发现 PostgreSQL 16.15、MinIO RELEASE.2025-09-07T16-13-09Z 正在其他项目容器运行；仅作为版本与配置参考，不表示已验证目标 Linux 服务器。独立项目资源、队列与执行适配方案见 DEV-PLAN.md。其余未确认业务默认仍按第 11 节管理，不能因技术栈确认而自动视为确认。

2026-09-08 原型交付阶段的已确认约束（历史背景，当前开发顺序见第 13 节）：前端以 art-design-pro 为底座，直接复用其布局、导航、主题和既有组件，不另造底板。当时仅交付 HTML 交互原型供评审，不启动正式项目开发；生成、Skill、用户与数据均为演示，不连接 CLI 或服务器。后续已启动正式开发，当前约束仅描述历史原型阶段的范围，正式实现以 `hengxin-smart-image/frontend/` 为准。

保留模板库、后台任务、结果预览的业务方向；新增壁纸和文字处理类型、Skill 绑定、整套/单张意见及成品库。原稿固定 20 张、多角度素材限制、设计专属模板权限和手工区域配置不自动继承。

业务交付包内的原始设计资料可以作为视觉参考，但功能已不完全对应本 PRD，需要后续更新导航、模板配置、结果详情和归档页。当前仅登记影响，不改动原始设计文件。Design-Brief.md 与 DEV-PLAN.md 已承接这些变化；正式工程进度统一见本文第 0 节及 DEV-PLAN.md，不以原交付包的页面范围判断当前实现。原业务交付包仅作参考，不覆盖当前 PRD。

## 13. 管理中心、四角色与钉钉登录详细设计

以下统一作为本 PRD 的组成部分维护，已确认需求与建议细则的状态保持不变。

### 已确认与建议边界

已确认四角色：系统超级管理员（用户本人）、设计主管、设计人员、运营人员。Skill 包提交、维护及服务器安装全部归超级管理员；钉钉电脑端企业内部应用与电脑 Web 共用钉钉身份登录。

已确认设计人员、运营人员均使用完整业务流程：模板创建维护、三类图片处理、整套/单张返工、下载、归档与个人调用统计；两者共用同一权限集合和数据范围规则，仅岗位名称不同。设计主管查看全员任务及统计；模板上传保存后直接可用；四角色均可查看和删除全员模板、任务与成品。不新增生成结果的主管审批流。第一期不做复杂上下文管理，新业务任务新会话，同任务返工续接，所有执行走异步队列。

### 前端依据与导航

以 `hengxin-smart-image/frontend/` 当前正式源码为基础，保留 art-design-pro 布局、侧栏、标签页及组件。使用现有 ArtTable、统计卡片、图表、Element Plus 表单和状态标签，新页面无需重新制作底板。原来的图片处理/任务中心/模板库/成品库保持业务职责。

```text
图片处理
  替换壁纸 / 替换商品 / 替换文字
任务中心
模板库
成品库
管理中心（按权限展示）
  调用统计
  执行监控
  用户与角色
  Skill 管理
  系统配置
```

设计人员和运营的调用统计仍采用个人视图；设计主管和超级管理员可查看全员调用统计。所有角色均可查看全员模板、任务与成品；第一期不需要为这些资源配置团队查看或删除范围。

### 权限矩阵

四角色均可编辑全员模板、返工和归档全员任务（2026-09-09 用户明确确认）。共享业务资源权限不扩展为全员维护 Skill、系统配置、分配角色或查看全员调用统计；实际操作者独立记录。

| 功能 | 系统超级管理员 | 设计主管 | 设计人员 | 运营人员 |
|---|---|---|---|---|
| 创建三类处理任务 | 可 | 可 | 可 | 可 |
| 查看模板、任务、成品 | 全部 | 全部 | 全部 | 全部 |
| 删除模板、任务、成品 | 全部 | 全部 | 全部 | 全部 |
| 返工任务（已确认） | 全部 | 全部 | 全部 | 全部 |
| 创建模板 | 可 | 可 | 可 | 可 |
| 编辑/停用模板（已确认） | 全部 | 全部 | 全部 | 全部 |
| 模板上传保存后使用 | 直接可用 | 直接可用 | 直接可用 | 直接可用 |
| 绑定已发布 Skill | 可 | 可 | 可 | 可 |
| 上传/安装/更新/启停 Skill | 可 | 不可 | 不可 | 不可 |
| 成品浏览与下载 | 全部 | 全部 | 全部 | 全部 |
| 归档（已确认） | 全部 | 全部 | 全部 | 全部 |
| 调用统计 | 全员 | 全员 | 个人 | 个人 |
| 队列及业务异常 | 全局 | 全员任务 | 全员任务 | 全员任务 |
| CLI、依赖健康详情 | 可 | 只读总体可用状态 | 简要不可用提示 | 简要不可用提示 |
| 分配角色、禁用用户 | 可 | 不可 | 不可 | 不可 |
| 系统参数与钉钉配置 | 可 | 不可 | 不可 | 不可 |

角色是系统权限，不等同钉钉企业管理员身份。第一期每人一个业务角色，超级管理员为独立最高权限；未分配用户保持待授权，不默认成为运营。角色与资源范围由后端校验，URL、API 和签名下载都不能绕过。禁用或降权后现有系统会话在下一请求重新校验。操作留存实际操作者，代他人返工不混淆调用归属。

### 管理中心页面设计

#### 调用统计

**调用统计建议口径**：

- 按 Asia/Shanghai 自然日统计，支持单日及日期范围；新建任务按创建时间归日，执行按实际开始时间归日。
- 显示新建任务数、首次生成/单张返工/整套返工执行次数、执行成功/部分失败/失败/超时、成功输出图片数、排队时间和执行耗时。一个任务初次生成后返工两次计 1 项任务、3 次实际执行，不等于 3 次模型内部请求。
- 一次实际 CLI 启动记一次 execution attempt；消息重复投递未启动 CLI 不增加执行次数，真实重新启动另记 attempt。记录任务创建人和本轮操作者，全员调用统计按实际操作者归属，避免管理员代操作算给运营。
- 队列中未启动的不计实际调用；跨日未完成执行显示进行中，完成后更新所属开始日结果。成功率为成功次数 / 已结束执行次数，部分失败单独展示且计入分母，未结束不入分母；零分母显示无数据。输出图片数按成功持久化的版本计数，返工新版本也计入并标明口径。
- Token 仅展示 CLI 可靠返回的实际 usage，字段缺失显示“未提供”，不计为 0；同轮累计事件不得重复相加。预检失败可能无 usage，不能推断免费。费用和账号剩余额度不从 Token 猜算，第一期不承诺账单或配额看板。

顶部显示日期范围、用户、处理类型筛选；个人角色不出现全员选择器。中部展示新建任务、执行次数、成功/失败、成功产出图片及排队/执行耗时；下方按人员列日汇总，点击展开调用明细和关联任务。

#### 执行监控

超级管理员看到 Worker 心跳/最后检查时间、队列数、运行数、并发上限、CLI 版本与配置就绪情况、最近调用成功或错误，以及 PG/Redis/MinIO、临时目录空间。表格列运行任务、操作者、会话标识、阶段、已运行时间和脱敏错误。

CLI 每轮结束退出是正常行为，空闲不报离线。Worker 失联、依赖不可达、认证拒绝、限流、超时分别表达；历史成功不能代替当前健康检查。过期检查结果显示未知。健康检查不周期性发起生成或消耗模型调用。

设计主管看到全员队列与失败任务、总体服务可用性，不暴露服务器路径、凭据和原始日志。普通角色可在任务中心查看全员任务及业务状态。

#### 用户与角色

从已验证的钉钉成员建立用户，显示姓名、部门、角色、状态及最近登录时间。超级管理员分配四角色，启停系统账号；不提供自助提权。初始超级管理员通过部署配置指定企业成员标识，不采用“首个登录者就是管理员”。

建议第一期允许目标企业成员首次登录生成待授权记录，等待超级管理员分配角色；首次入职不会自动获得业务权限。企业成员资格在登录时校验；本地禁用即时生效。离职成员检测需在接入通讯录权限后通过定期校验或事件同步撤销会话，建议目标在成员变更后 15 分钟内生效，并在联调验证权限与接口频率。

#### Skill 管理

2026-09-21 已确认迭代（轻量目录管理，替代下方人工版本登记操作）：
- 管理员维护固定根目录下 `<Skill标识>/SKILL.md` 和完整配套文件；平台提供“同步 Skill”，由真实 Worker 扫描与检查，不执行 Skill 脚本或触发生图，不要求填写语义版本。仅超级管理员同步、启停、设默认、移除登记。
- 每个 Skill 一条记录，展示名称、SKILL.md 自带 description（列表摘要可展开全文）、处理类型、可用/停用/异常及默认标记。不显示版本列表、版本号输入、更新版本、版本切换。描述在模板选择时可见。无法确定处理类型时明确要求管理员选择壁纸/商品/文字，不从自然语言猜测。
- 同步成功的新技能直接可用，已有手动停用状态保持；失败或文件缺失明确显示异常，不以旧内容伪装同步成功。不覆盖其他正常技能的结果，同步中的重复请求合并/互斥。
- 新任务使用最近一次同步成功的内容；平台自动保存不可变的完整执行快照，管理员不管理快照版本。模板和模块默认绑定Skill身份，新任务取其最新可用快照；已提交/排队任务及旧任务返工继续绑定原内容，不受文件覆盖、同步和停用影响。没有可用快照时新任务明确拒绝，不静默换Skill。
- 移除仅撤销平台登记，不删除服务器文件及任务快照；有模板/模块默认依赖时先解除绑定，历史任务不阻止移除但仍可返工；重新同步仍存在的目录可再次发现。旧版本目录/ZIP和历史绑定保持兼容，不自动删除旧文件，也不要求既有任务迁移。
- 校验与快照保留目录越界/软硬链接/特殊文件/超限拒绝、文件树一致性、隔离只读访问和权限边界；快照存储不可用时不得发布可用状态。验收同步增改、停用保留、描述全文、缺失/异常/未知类型、权限、并发、快照字节及旧任务返工、旧数据兼容和移除保护。

下方2026-09-17版本流程为历史兼容说明，新管理入口以上述轻量目录管理为准。

2026-09-17 最终回复收图（替代当天较早的后台 manifest 交付协议）：两个业务 Skill 保持原样，管理员维护完整版本；任务将绑定版本只读挂载到 `/work/skills/{Skill标识}/` 及任务私有 HOME 的 Skill 发现目录。提示词使用 `$Skill名称`，仅包含本轮目录、按顺序排列的底图与素材、修改要求和可选补充意见；不追加原生输出限制、manifest 或内部 JSON。壁纸要求为“把这张手机屏幕素材替换掉图片的壁纸与镜头，其他一律不允许有任何改变”，商品/文字按各自要求；返工保留当前图、原图和修改意见，意见仍作为引用数据。

后端以本次 CLI 调用完成前的最终 assistant 答复中明确交付的本地图片引用作为唯一成品列表，按清晰编号或展示顺序对应本轮底图；只接收完整数量，不猜测缺失槽位。候选图、工具输出、较早过程消息与旧轮次答复均不作为交付。允许经过 Skill 合成、修复的图片，不再要求成品字节与原生回执一致，不要求改 Skill 或生成平台专用清单。图片必须位于本轮 work（排除输入、当前图和 Skill 目录）或本会话本轮新增的原生图片中；阻止跨任务/历史文件、链接与重复文件，校验格式、解码和大小，保留基本尺寸安全限制，不将平台技术校验声称为视觉效果合格。最终答复缺失、不完整或文件无效时明确失败，无权收图或取消时不得发布；历史 manifest 协议仅用于升级前已有执行的恢复，新执行与返工统一使用最终回复协议。

2026-09-17 已确认迭代（v0.25，替代下方历史 ZIP 新增/安装流程）：管理员人工维护服务器 Skill，平台登记并验证本地发布版本；新版本不再通过网页上传 ZIP 或由平台安装。保留旧 ZIP 来源及其任务、模板绑定，历史版本继续可执行与返工，不在本轮自动迁移或删除旧包。

- 发布位置由部署配置指定固定根目录，按 `<Skill标识>/<语义版本>/SKILL.md` 定位；后台只填标识、版本、处理类型和说明，不接受任意绝对路径。允许先登记再部署，初始待部署；登记不等于可用。
- 管理员发布版本后不原地修改，变更使用新版本。目录和文件应由管理员拥有且执行用户不可写；拒绝链接、越界、特殊文件及大小超限。校验完整文件树并记录独立内容校验值，旧 ZIP 校验值含义保持不变。
- 由真实执行 Worker 异步检查部署：元数据、可读性、声明依赖及实际隔离环境的访问；检查不得执行 Skill 自带脚本或触发 AI 生图。检查通过进入已验证（未启用），管理员显式启用；重新检查已启用版本不自动恢复启用。已登记校验值固定，内容变化须恢复原内容或登记新版本，不能重新检查覆盖旧校验值。
- Skill 管理保留分组版本列表、模块默认绑定及审计，提供登记、检查部署、启用/停用、移除登记；移除仅限未被模板、任务、模块默认引用且无检查作业在途的本地版本，不删除服务器文件。失败明确显示原因，页面保留加载、空态及权限限制。
- 每轮执行前校验任务冻结版本及内容，将该版本只读挂载到 `/work/skills/{Skill标识}`；图片仍为任务专属目录，不复制/解压本地 Skill，不把整个用户全局 Skill 目录暴露给任务。目录缺失、变更或依赖不可用时明确失败，不回退到其他版本。
- 模板保存和任务提交固定版本；默认切换不改已有模板/任务，旧任务返工仍使用旧版本。停用禁止新任务使用，不静默替换历史绑定；历史返工仍验证原内容。不同任务的 Home、会话、素材、结果和控制日志隔离保持不变。
- 验收覆盖未部署、元数据错配、目录越界/链接、依赖缺失、内容漂移、角色越权、重复登记、检查并发、引用删除保护、新版本切换及旧任务返工、ZIP 兼容和只读挂载。真实部署校验与生图效果验收分别记录。

以下及上方 2026-09-17 的人工版本登记规则为历史实现说明；当前维护与绑定以本节 2026-09-21 Skill 轻量管理规则为准。

仅超级管理员进入。显示三种处理类型的当前 Skill 版本、包校验和、安装节点、安装状态及最近变更。提供上传、校验、安装、设为可用、停用和版本切换；不让设计/运营上传可执行包或改服务器环境。

Skill列表按名称与处理类型分组，同一Skill只显示一行；主行显示版本数量、最近上传版本和模块默认版本（没有则明确显示未设为模块默认），不将最近上传或可用状态等同于默认。展开显示全部版本，逐版本保留安装状态、引用情况、校验记录、安装/重试/启停操作。刷新数据保留展开状态。主行提供“更新版本”入口，复用上传流程并预填处理类型，展示所选Skill名称、当前版本及同名新包提示；新版本号由管理员填写，不自动推测。通用上传入口与更新入口每次打开均清空旧文件、版本号和错误。更新通过包内同名元数据归入同一Skill，上传成功后仍需安装；新版本不自动替换模块默认绑定、模板固定绑定或历史任务引用。

包可持久化到 MinIO，但“包已上传”不等于“服务器已安装”。安装由受控后台作业异步完成：校验包 → 准备版本目录 → 检查依赖与可读性 → 成功后发布。失败保留旧可用版本。队列中的任务冻结 Skill 版本，在运行任务不受更新影响；被任务引用的版本不能直接删除。首期单 Linux Worker，页面如实显示安装状态，不做无依据的集群管理界面。

#### 系统配置

仅超级管理员修改执行并发、执行超时、上传限制和默认 Skill 绑定。所有变更有校验和审计，仅影响后续轮次。钉钉区域展示企业/App 标识、回调域名和配置检测结果；AppSecret、CLI 凭据保留部署环境，不返回前端。第一期不提供网页任意终端。

实现约束（2026-09-16 本机阶段）：网页并发上限不得超过部署 Worker 容量，不在线扩 Celery pool；旧轮次冻结的超时及配置版本保持不变。超时 60–3600 秒且不超过部署执行上限，页面展示实际可配置范围；部署上限低于 60 秒时只读并提示调整部署。单张上传 1–10 MiB；钉钉标识/检测只读，仍由部署环境管理。配置采用版本冲突检测；默认 Skill 变更与现有管理入口共享绑定和审计。监控始终保留当前运行与待核实任务，其余排队/异常展示最近 100 条并提示总数与截断；读取失败时数量为未知，不以 0 代替。

### 钉钉双端登录

一套 Vue 前端、一套 FastAPI、一套用户与权限表，设计为钉钉企业内部网页/H5 应用。不是开发两套产品，也不是小程序重写。

| 入口 | 登录体验 | 后端处理 |
|---|---|---|
| 钉钉电脑端工作台 | 打开应用，容器获取临时免登授权码 | 验证授权码，核实企业成员及应用可见范围，建立系统会话 |
| 普通电脑浏览器 | 点击钉钉登录，进入官方网页授权/扫码确认流程 | 验证回调及 state，获取可信身份、验证企业关系，建立同一系统用户的会话 |

浏览器能用钉钉身份登录不等于一定静默免登；扫码/授权是否需要确认由钉钉登录状态和授权流程决定。钉钉客户端内部免登失败时给出重试和说明，不静默套用演示身份。

账号映射采用后端验证的企业身份（目标 corpId 与成员 userId），Web 返回标识通过官方接口映射到目标企业成员；unionId 仅作经过验证的关联依据。不同入口返回的标识不能直接当作相同值，不以姓名或手机号猜测合并。

使用对应企业网页应用的 SDK/授权流程；不误用小程序 getAuthCode。钉钉官方 JSAPI Explorer 展示 requestAuthCode 微应用 PC 支持，但具体 SDK 版本、调用参数和码交换链路在接入阶段按企业应用类型固定并实测。授权码不持久化为登录凭据；AppSecret 和钉钉访问令牌仅服务端持有。

网页回调使用后端固定 HTTPS 路径 `/api/v1/auth/dingtalk/callback`，不使用 Vue hash 片段作为 OAuth 回调。验证短期、一次性 state 及浏览器绑定，避免回调重放和任意跳转；完成后跳转允许的站内页面。系统会话采用安全 Cookie，并实测钉钉桌面容器 Cookie 行为。用户关闭钉钉窗口不会取消已受理的异步任务。

#### 接入前需要的环境资料

- 目标企业 CorpId、企业内部应用 Client ID/AppKey、AgentId、应用可见人员范围。
- 服务端安全配置 AppSecret；按所选登录及成员查询流程申请最小必要权限。
- 生产 HTTPS 域名、PC 应用首页、网页授权回调及相关域名配置；不能把 localhost:3007 当正式钉钉应用地址。
- 初始超级管理员的钉钉企业成员 userId 列表；支持多个初始超级管理员，主管查看全员，无需提供团队范围。
- 实际 Windows/Mac 钉钉版本与目标浏览器，用于确定验收矩阵。以上配置在联调前提供，不阻塞文档设计。

#### 双端验收

钉钉 PC 与 Chrome/Edge 都覆盖首次登录、再次访问、过期重登、拒绝授权、未授权成员、禁用角色、同一身份映射、上传、预览、单图/ZIP 下载、异步提交、返工及归档。重点实测容器中的下载打开方式、窗口大小、Cookie 与外部授权回跳；不能只测浏览器后宣称钉钉兼容。Mac 若有业务用户则纳入实机矩阵。手机适配不在本期。

### 开发接入位置

已确认开发优先级：先完成全部前端，再开始后端开发。Phase 1–4 承接现有 art-design-pro，完成三种图片处理、模板库、任务与返工、成品库、管理中心及登录状态页面；使用独立模拟接口体验完整交互，不依赖 FastAPI、数据库、MinIO、CLI 或钉钉配置。每完成一组页面即提供预览，全部前端完成后再进入 Phase 5 后端工程。

Phase 5–11 完成后端基础、文件与模板、异步队列、Codex 执行、同会话返工和归档，并逐模块接入已完成前端；Phase 12 接钉钉真实登录与四角色授权，Phase 13 接管理中心真实统计和监控，Phase 14 完成双端联调部署。登录页面先完成，真实认证仍在后期接入。

前端完成指页面、交互和各状态在模拟接口下通过验收，不代表真实生成、持久化、授权或监控已完成。前端先定义数据类型和接口契约，模拟与真实接口共享契约；模拟环境明确标识，生产禁止模拟模式。后端从文件与任务阶段记录稳定用户 ID、资源所有者和每轮操作者；本地联调使用服务端固定测试身份，生产禁止开发身份，真实接入时不自动将测试数据归入钉钉成员。具体交付和验收见 DEV-PLAN.md。

### 官方参考

- [钉钉企业 H5 应用免登示例](https://github.com/open-dingtalk/h5app-auth-demo)：JSAPI 授权码、服务端用户识别及 PC 工作台应用配置。
- [钉钉微应用 requestAuthCode 支持表](https://open.dingtalk.com/tools/explorer/jsapi?id=11723)：微应用 PC 免登能力，实施时核对 SDK 和客户端版本。
- [钉钉网页登录教程](https://open.dingtalk.com/document/orgapp/tutorial-obtaining-user-personal-information)：浏览器登录入口；页面为动态文档，本轮正文抓取有限，具体接口参数须在联调前再次验证。
- [钉钉官方教程目录](https://open.dingtalk.com/tutorial/)：区分内部免登与第三方网站扫码/账号授权场景。


## 2026-09-21 · 需求补充（仅本地框架 Demo）

用户要求将图片优化原型拓展到 Demo + 假数据级别。范围、拆分、边界和验收以 [框架 Demo 计划](docs/FRAMEWORK-DEMO-PLAN.md) 为准。沿用当前品牌和正式前端，新增持久化假数据、模板维护、三类模拟任务、版本返工、归档与管理演示。正式工程和既有阶段验收不因本次 Demo 交付改变。


## 2026-09-21 · 用户确认基于现有框架迁移 Demo

用户确认采用现有 Vue 3、Element Plus、Art Design Pro 框架内的独立Demo模式，复用真实布局和业务组件，假数据隔离于生产API。此决定替代上一版独立HTML作为后续交互评审实现方式；旧原型已清理。范围及验收见 [框架Demo计划](docs/FRAMEWORK-DEMO-PLAN.md)。不改变生产业务接口和既有阶段验收结论。
## 2026-09-22 生图等待反馈

任务结果尚无图片且状态为排队中/执行中时，以轻量动态画布替代空盒占位：排队轻呼吸，执行保留背景光晕流动与中心图标呼吸（2026-09-22按用户要求取消向下扫描光带），显示真实任务状态及图片序号，不模拟百分比或推断内部生成阶段。失败及终态停止动画，已有图片和历史版本保持可查看。遵循减少动态效果偏好，离屏与隐藏页暂停。参考 21st.dev AI Loader、Liquid Loader 的动效方向，在 Vue 中独立实现。
## 2026-09-22 紧凑素材上传

三类任务上传区空态保留大拖拽框；存在图片（含上传中、失败）时改为104px缩略图网格及追加入口，支持整区拖拽追加、放大切图、移除与重试。超过两行可展开，失败或上传中不折叠隐藏。移除最后一张恢复空态。模板排序上传与单张返工截图入口保留原行为。

## 2026-09-22 正式前端 UI 发布范围

用户已批准当前 UI 改进同步至正式 Vue 前端并部署。模板/素材/结果/成品完整缩略图及整组预览、模板选择与取消、任务快照参考图、生图等待反馈和紧凑上传均为正式共享组件要求；不再限于 Demo。独立 Demo 保持本机 3010 与假数据隔离，正式构建默认 production 模式连接真实 API。发布完成以验证和部署记录为准，不改变真实业务验收边界。

## 2026-09-22 · 核心交互审查前五项优化（用户已确认）

1. 任务列表缩略图完整等比显示，点击或键盘打开当前任务结果图集，左右切换不触发详情；列表名称/查看详情仍打开任务。
2. 更换模板卡片名称、张数、当前状态置顶；选择态预览紧凑、按钮与身份可同时识别，保留完整图集与取消更换。1280×900 验收。
3. 任务中心增加“全部／我的”筛选与创建人线索，默认全部，当前用户由服务端认证确定；在分页前筛选，列表及统计范围一致，URL 保留范围且变更重置页码。长编号单行省略，支持完整值查看与复制；1280筛选有明确对齐。全员查看/操作权限不变。
4. 单图修改桌面左右图文对照、窄屏上下排列，意见不因预览切换丢失；问题截图限制统一引用实际数量（此入口1张），保留原版本与单图修改语义。
5. 部分失败集中说明处理结束与成功/失败数量，数量以当前执行槽位结果为依据，不把保留的旧版本算作本轮成功；重试范围可理解，保留已有图查看下载及原幂等/恢复逻辑，不伪造进度。

范围为审查01–05，06–09暂不实施。正式前端与Demo共享；本轮交付本地开发效果，生产发布另行记录。

## 2026-09-22 创建任务首屏布局修正
用户反馈整页高度过大。三类创建页采用左侧模板/素材、右侧任务信息/提交布局；压缩重复预览和标题留白，Demo 调试工具默认折叠且可展开。1366×768、1280×800下默认选定模板的空素材态及单图态，任务名称、修改要求和提交按钮无需滚动可见；多图和模板选择展开允许内容滚动，不能遮挡提交。窄屏按自然顺序堆叠，不缩放整页、不隐藏错误和必填项。上传空态改为紧凑拖拽区，替代此前“大拖拽框”要求。

## 2026-09-22 套图展示与模板选择布局细化
用户要求默认展示更多套图、组件高度协调、更换/取消不生硬、每行3套。默认选定模板展示前6张完整等比图片及总张数，点击任意图可看整套；大屏限制内容最大宽度，模板和任务信息区统一首行高度，使用16px区间节奏。更换模板在独立选择对话框进行，不推走上传/表单；关闭、取消保持原模板与输入，明确选择才应用。桌面选择器3列，每页6套；窄屏2/1列，搜索、分页、失败重试保留。真实可用模板不足3套时如实展示，不复制假模板填格。保持1366×768和1280×800核心输入/提交首屏可见。

## 2026-09-22 模板库四列完整卡片
用户要求模板库桌面一行4套，首行卡片及底部操作完整可见。仅调整模板库布局：桌面>=1200px为4列，900–1199px为3列，600–899px为2列，更窄为1列；预览完整等比、固定紧凑高度，保留整组放大与名称、状态、版本、使用/配置/历史/删除。1920×1080、1366×768、1280×800验收首行完整，长文字允许卡片自然增长且全文可查看，不固定高度裁切卡片；不改变成品库和模板选择面板。

## 2026-09-22 成品库四列完整卡片
用户确认成品库同样采用桌面4列与完整卡片方案。复用模板库的布局样式与响应式断点，预览等比，名称、归档时间、查看/下载/删除完整保留；首行在1920×1080、1366×768、1280×800可见，长文本自然增长。保持成品详情、归档快照、来源任务、下载和删除确认逻辑不变，同时回归模板库布局。

## 2026-09-22 任务中心首屏密度
用户要求第一眼展示更多任务。去除装饰眉题并压缩页头，将全员/我的范围与四项真实统计合成一条紧凑摘要；列表筛选保持清晰分组。任务行改为两行身份信息（名称；创建人/SKU与可复制完整编号），44px等比图，行高约60px，查看和删除同排且删除确认保留。状态、真实进度、时间、权限、分页和URL筛选语义不变。正常任务数据足够时1920×911首屏目标至少8条，1366×768至少6条，1280×800至少5条；保留缩略图大图、完整编号复制及窄屏可访问性，不缩放整页或隐藏关键操作。

## 2026-09-22 · 已有结果收取阻塞修复

待核实状态只阻止新的生成调用，不能阻断已有结果的下载保存。复现、修复、测试与生产验证按 [修复计划](docs/API-IMAGE-COLLECTION-FIX.md) 执行。
