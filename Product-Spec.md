# 产品需求文档：恒信 AI 换套图系统

## 0. 文档状态与依据

最新导航确认（2026-09-08）：一级菜单“图片处理”，下设三个二级菜单“替换壁纸”“替换商品”“替换文字”。各自打开原有独立页面；取消页内三标签方案。此前“两个二级菜单”的表述已由用户纠正，不再待确认。

- 版本：v0.13；日期：2026-09-09；状态：Phase 1–2 已验收，Phase 3 任务、返工与成品库前端技术验证通过、待用户验收，含明确标注的待确认默认方案。
- 产品范围以用户当前会话确认内容为准；业务交付包作为参考，原始文件不改动。
- 原交付包：`C:/Users/82358/Downloads/设计部-国内生图资料包/`。
- 本文统一记录功能、流程与验收依据；“已确认”表示用户明确要求，“建议默认”不表示用户已批准，“待定”不允许开发自行扩展。
- 用户明确要求：Skill 暂未提供，不阻塞 PRD；图片效果与效果验收本轮暂不讨论。本文仅定义业务行为和平台功能验收，不承诺生成质量、成功率或生成时长。
- 本文不代表服务器已检查、CLI 已安装或容量已验证，不指定尚未调研的框架、模型和 API 版本。

## 1. 产品背景与目标

设计人员和运营人员需要复用已有商品主图、副图等图片，完成壁纸、商品主体或文字替换。系统通过图片处理一级菜单下的三个页面收集素材及要求，由服务器上的 Codex CLI 调用指定 Skill 执行，返回图片供预览、反馈返工、下载和归档。

已确认用户角色为系统超级管理员（项目负责人本人）、设计主管、设计人员、运营人员；初期约 20 人，后期最多约 100 人（已确认）。该人数是使用规模，不等于同时运行 20 或 100 个生成任务。

业务目标是形成“复用模板 → 上传素材 → 生成 → 修改 → 下载 → 归档”的完整流程。原业务资料中的 20–30 SKU/日、30 分钟完成和 100% 无需修图不作为本版承诺。

## 2. 第一期范围

| 编号 | 功能 | 优先级 | 确认状态 |
|---|---|---|---|
| SCOPE-001 | 图片处理内的壁纸模式与对应 Skill | P0 | 已确认 |
| SCOPE-002 | 图片处理内的商品模式与对应 Skill | P0 | 已确认 |
| SCOPE-003 | 图片处理内的文字模式与对应 Skill | P0 | 已确认，不细拆复杂交互 |
| SCOPE-004 | 设计/运营上传套图模板并绑定已发布 Skill，选择模板生成 | P0 | 已确认 |
| SCOPE-005 | 新任务独立 CLI 会话、进度及图片结果回显 | P0 | 已确认 |
| SCOPE-006 | 整套或单张修改意见与重新生成 | P0 | 已确认 |
| SCOPE-007 | 预览下载、独立成品归档库 | P0 | 已确认 |
| SCOPE-008 | 四角色、钉钉双端登录、资源权限与历史保护 | P0 | 四角色、双端登录及全员查看/删除已确认 |
| SCOPE-009 | 管理中心：每日调用统计、执行监控及系统配置 | P0 | 用户要求设计，细则见第 13 节|

本期不主动扩展（前两项已确认，其余为建议范围边界）：

- OUT-001：不把换文字发展为复杂排版编辑器。
- OUT-002：本轮不制作 Skill，不进行图片效果评测。
- OUT-003：生成结果不新增主管审批流，修改意见由使用者提交；模板上传保存后直接可用，不设主管发布审批。
- OUT-004：不直接沿用 PSD 图层编辑、涂抹遮罩和落地点配置工具。
- OUT-005：不包含商品平台自动上架、移动端完整编辑和多 SKU 批次编排；如保留原稿批次功能需另行确认。

## 3. 用户任务与流程

| 任务 | 用户目标 | 对应流程 |
|---|---|---|
| TASK-001 | 沉淀可复用模板及处理规则 | FLOW-001 |
| TASK-002 | 用新素材完成壁纸或商品替换 | FLOW-002 |
| TASK-003 | 对图片进行文字替换 | FLOW-003 |
| TASK-004 | 提交整套或单张修改反馈 | FLOW-004 |
| TASK-005 | 下载并保存满意结果 | FLOW-005 |

### FLOW-001：模板准备

设计/运营进入模板库 → 新建模板 → 上传现有商品图片 → 选择适用功能 → 绑定超级管理员已安装发布的 Skill → 保存后直接可用（无需主管发布） → 所有系统用户可在相应入口选择使用。无可用 Skill 可先存草稿；上传和安装 Skill 不属于此流程。

建议默认：模板允许先保存草稿；未绑定可用 Skill 时提示“暂不可生成”。图片允许排序和移除；模板更新形成新版本，不改变已提交任务。

### FLOW-002：壁纸或商品替换

进入图片处理并选择壁纸或商品类型 → 选择适用模板 → 上传替换素材 → 提交 → 持久保存业务任务并返回受理标识 → 排队 → Worker 领取后创建独立 CLI 会话并执行 → 回显图片结果 → 进入预览与修改。

模板缺失、Skill 未就绪或素材无法读取时，说明具体原因。建议默认：每次提交针对一个商品/一组素材，已确认输出张数跟随模板实际图片数；文字任务跟随输入图片数，不强制 8＋12 张。

### FLOW-003：文字替换

进入替换文字 → 上传待修改图片并提供修改要求 → 异步受理并排队 → Worker 调用对应 Skill → 展示结果 → 可修改、下载、归档。

上传图片加自然语言要求已确认；不强制使用套图模板，不展开文字框选、字体参数等复杂交互。

### FLOW-004：反馈返工

查看结果 → 选择整套或某一张 → 填写修改意见 → 异步提交返工并排队 → Worker 续接原会话 → 展示新结果。

单张返工仅更新目标图片。建议保留历史版本，在同一业务任务内记录修改轮次；已确认首次生成及后续整套、单张修改均续接该任务同一 CLI 会话，同一运营提交新任务则创建新会话。

### FLOW-005：下载与归档

运营预览后可以下载；满意后执行独立归档操作 → 成品库显示归档结果 → 后续查看与下载。

建议默认：单张下载与整套打包并存；归档保存选定结果版本，后续返工不覆盖已归档文件。

## 4. 功能需求

### REQ-001：一级菜单下的三种处理页面（TASK-002/003，FLOW-002/003）

- 侧栏一级菜单为“图片处理”，下设替换壁纸、替换商品、替换文字三个二级菜单，各自进入独立页面并调用相应 Skill。
- 壁纸模块修改屏幕内图案，商品模块修改模板中的商品主体，文字模块按要求修改文字；具体处理方法由 Skill 承载。
- 不能把某类任务误送给另一类 Skill；允许共用任务与成品基础设施。

### REQ-002：模板与 Skill 关联（TASK-001，FLOW-001）

- 设计人员和运营人员均可上传已有商品图片建立模板，绑定已发布 Skill。
- 建议字段：模板名称（必填）、适用功能（必填）、有序图片列表（必填）、Skill 引用及版本（执行必需）、备注（可选）。
- 建议默认：每个模块具有默认 Skill，套图模板可绑定专用 Skill；已确认仅系统超级管理员上传、安装和维护 Skill，其他角色只能选择可用版本。实际包格式按技术方案验证。
- Skill 尚不存在时允许准备模板草稿，但不得将模拟执行显示为真实生成成功。
- 已确认全员可查看和删除模板；上传保存后无需审批即可使用（生成仍需可用 Skill）。建议支持搜索、编辑、停用；被历史任务引用的版本保留。

### REQ-003：素材与提交（TASK-002/003，FLOW-002/003）

- 壁纸和商品任务需要功能类型、模板和替换素材；文字任务需要待修改图片和自然语言修改要求（已确认）。
- 提交前检查文件是否为空、可读取及是否属于支持类型。已确认 JPG/PNG/WebP、每张不超过 10 MiB、每组最多 20 张，模板和输入素材分别计数。
- 已确认任务名称必填，SKU 可选。
- 建议防止重复点击创建重复任务；已提交任务冻结输入及模板/Skill 版本。

### REQ-004：任务执行与进度（TASK-002/003，FLOW-002/003）

- 已确认全员可查看和删除任务，记录实际删除操作者；删除不破坏仍被使用的历史文件引用。

- 新生成任务通过服务器上的 Codex CLI 创建独立会话，固定调用指定 Skill。
- 系统需要保存业务任务和 CLI 会话的关联，以及输出图片与模板图片的对应关系。
- 前端展示真实执行状态；只有获取到可读取的输出图片才可展示为该图片生成完成。
- 已确认采用异步后台队列：首次生成和返工提交在校验并持久保存后返回任务/轮次标识，不等待 CLI 或生成完成；关闭浏览器或退出登录不终止已提交任务，前端独立查询状态和结果。
- 建议并发数量可配置，超过运行名额时排队；不得按账户总人数直接启动同等数量进程。
- 任务进度来自执行事件或已完成图片数；没有可靠比例时只显示处理阶段，不编造百分比。

### REQ-005：审阅与修改（TASK-004，FLOW-004）

- 支持整套预览和单张查看，整套与单张均可填写修改意见。
- 返工输入包含指定 Skill、目标图片范围、相关素材与本轮意见。
- 单张返工不得更新其他图片；建议保留旧版本，新结果成功后才更新当前版本。
- 建议同一任务的修改请求串行执行，避免整套与单张修改互相覆盖。
- 不另设主管审批节点。

### REQ-006：下载与归档（TASK-005，FLOW-005）

- 图片结果可从前端下载，满意结果可归档到独立成品库。
- 已确认全员可查看和删除成品；建议支持名称搜索、功能类型筛选、预览、单张下载与整套打包。
- 建议归档关联任务、结果版本、创建人及归档时间；重复点击同一归档动作不重复创建记录。
- 建议部分失败时允许查看、下载已成功的单张，但不把残缺结果标为完整套图；默认全部完成后才整套归档。
- 建议默认保持源图尺寸与格式，具体格式兼容性、命名和压缩要求待定。

### REQ-007：四角色与管理中心

- 已确认设计人员与运营人员拥有完全相同的功能权限及数据范围规则，均为日常生产使用者；角色名称仅用于岗位分类和统计，不限制设计人员只能测试模板。

- 使用第 7 节四角色及第 13 节中的数据范围；提供用户角色分配、个人/全员调用统计及 Codex 执行监控。
- 系统配置仅超级管理员可修改；设计主管的业务管理权限独立于系统配置权限。调用次数按真实执行 attempt 统计，与任务数和模型内部请求数区分。

### REQ-008：钉钉双端登录

- 同一套现有 Vue 前端支持钉钉电脑端内嵌企业网页应用和电脑浏览器。前者使用钉钉容器免登，后者使用钉钉网页授权/扫码登录；两入口均由后端验证身份并创建系统会话。
- 按目标企业成员身份绑定，不按显示名匹配。首次未分配角色显示待授权，企业外成员、已禁用账号不可进入业务。超级管理员由部署时明确指定的成员身份初始化，禁止首位登录者自动提权。
- 授权失效、拒绝、钉钉不可用有可重试提示；上传、下载、预览、异步提交、返工及归档在两端验证。仅桌面范围，不扩展手机布局。

## 5. 页面与状态

规划导航：图片处理、任务中心、模板库、成品库、管理中心。前四项为现有原型；管理中心按角色授权展示，尚未实现。图片处理一级菜单可展开或收起，包含替换壁纸、替换商品、替换文字三个二级菜单。Skill 管理位于管理中心，仅超级管理员可操作；日常执行只选择已发布版本。

| 页面 | 主要操作 | 空状态 | 加载/处理中 | 错误状态 | 成功状态 |
|---|---|---|---|---|---|
| 壁纸/商品入口 | 选模板、传素材、提交 | 提示选择模板和上传 | 上传进度/提交中 | 指出缺失项或不可用 Skill | 进入任务详情 |
| 文字入口 | 传图、填要求、提交 | 提示上传 | 上传/提交中 | 指出输入问题 | 进入任务详情 |
| 模板库及编辑 | 创建、绑定、预览、维护、全员删除 | 创建首个模板 | 保存中 | 保存失败保留草稿 | 保存反馈 |
| 任务中心 | 筛选、查看全员详情、删除 | 创建任务入口 | 排队及执行阶段 | 失败原因与重试入口 | 待查看结果 |
| 结果详情 | 反馈、下载、归档 | 尚无结果 | 目标图片重生成中 | 保留既有结果并提示 | 新版本可预览 |
| 成品库 | 搜索、预览、下载、全员删除 | 尚无归档成品 | 加载中 | 刷新/下载重试 | 查看归档图片 |
| 钉钉登录 | 容器免登或浏览器授权 | 登录入口 | 授权验证中 | 拒绝、失效、企业不符、待授权 | 进入获授权页面 |
| 调用统计 | 日期/用户/类型筛选、明细 | 暂无执行 | 查询中 | 查询失败/无权限 | 个人或全员汇总 |
| 执行监控 | 队列与环境状态 | 暂无执行节点 | 检查中 | 异常、失联、未知 | 就绪/空闲或执行中 |
| 用户与角色 | 分配角色、启停 | 暂无成员记录 | 保存中 | 校验失败/无权限 | 授权结果生效 |
| Skill 管理 | 上传、安装、版本启停 | 暂无 Skill | 上传/安装中 | 失败保留旧版 | 版本可用 |
| 系统配置 | 允许参数编辑及审计 | 未配置提示 | 保存中 | 越界/无权限 | 后续轮次使用新配置 |

建议统一无权限状态：不返回受限图片和资源，显示无权访问；登录过期后重新登录可回到原任务。

建议任务状态：排队中 → 执行中 → 待查看；执行异常进入失败/部分失败；重新生成进入执行中；归档作为结果记录，不代替任务执行状态。取消入口、超时及恢复策略待技术设计明确。

## 6. 数据模型与历史

| 实体 | 关键内容 | 关系 |
|---|---|---|
| 用户 | 标识、名称、角色、状态 | 创建模板、任务和归档 |
| 模板及版本 | 名称、类型、图片顺序、Skill 绑定 | 一个模板具有多个版本 |
| Skill 及版本 | 类型、包或引用、可用状态 | 关联模块、模板和任务 |
| 素材 | 文件、类型、尺寸、所属任务 | 一个任务关联多个素材 |
| 生成任务 | 类型、创建人、状态、输入快照、会话标识 | 关联执行轮次 |
| 执行轮次 | 修改目标、意见、起止时间、失败原因 | 产出图片版本 |
| 图片版本 | 原模板图标识、文件、轮次、当前版本 | 单张返工独立追踪 |
| 归档记录 | 任务、选定版本、文件、归档人及时间 | 保存满意结果 |
| 钉钉身份与角色 | 企业成员标识、角色、账号状态 | 两端绑定同一用户 |
| 执行尝试与 usage | 实际操作者、轮次、开始结束、结果、可空用量 | 一轮可关联多次真实尝试，统计按尝试去重 |
| Worker 状态 | 节点、心跳、检查时间、依赖状态 | 监控执行可用性 |
| 系统配置与审计 | 参数版本、操作者、变更前后值 | 后续轮次读取并冻结配置 |

这些字段是建议实现模型；数据库已确定 PostgreSQL，文件存储已确定 MinIO，具体表结构按开发计划实现。任务与素材须按任务隔离；不同运营的执行不能读错输入或覆盖他人输出。

## 7. 角色、登录与生命周期

- 已确认四角色：系统超级管理员、设计主管、设计人员、运营人员。具体权限矩阵见第 13 节；此前管理员/运营两角色建议由此替代。
- 已确认钉钉电脑端企业内部应用及电脑 Web 均支持钉钉登录；同一钉钉成员映射同一系统账号，角色由超级管理员分配，不从钉钉职称自动推断。正式方案不再默认使用本地用户名密码登录。
- 设计与运营均可创建维护模板且权限完全一致；模板上传保存后直接可用，无需主管发布。生成仍需绑定已安装可用 Skill。
- 所有已登录、已授权且启用的系统用户均可查看和删除所有人的模板、任务及成品，不按创建人或团队限制这两类操作；设计主管可查看全员任务和调用统计。
- 未提交素材和草稿可移除；可用模板可停用，删除不破坏历史任务引用。
- 模板、任务和成品均提供全员可用的删除入口，记录实际操作者；回收站形式及保留期限仍为建议，删除不破坏历史引用。
- 清理任务时不得连带删除仍被归档引用的文件。
- CLI 执行身份、凭据存放及可访问目录由技术设计确定；凭据不返回浏览器。

## 8. 部署与非功能要求

### 8.1 已确认的部署环境（用户提供，未实机检查）

| 项目 | 内容 |
|---|---|
| Codex CLI 部署位置 | Linux 服务器 |
| 主机名称 | racknerd-058889d |
| 发行版本 | Ubuntu 24.04 LTS |
| 内核 | 6.8.0-138-generic |
| 架构 | x86_64 |
| 初期使用人数 | 约 20 人 |
| 后期使用人数上限 | 约 100 人 |

尚无 CPU、内存、磁盘、带宽资料；不能仅凭系统版本判断承载能力。当前开发计划采用单机部署基线，实际容量与部署参数须在部署前验证。

### 8.2 非功能需求

| 类别 | 需求/建议目标 | 状态 |
|---|---|---|
| 规模 | 面向初期 20 人、后期约 100 人使用 | 已确认 |
| 并发 | 在线并发、执行并发、峰值提交量分别记录，执行名额可配置 | 建议；具体数值待定 |
| 可靠性 | 后台持久保存任务；服务重启可识别未完成任务，避免重复生成或错误标成功 | 建议 P0 |
| 故障处理 | 保留成功图片及失败原因；允许人工重试，自动次数与超时待定 | 建议 P0 |
| 数据保护 | 登录鉴权、任务隔离、文件访问校验，归档不被后续返工覆盖 | 建议 P0 |
| 兼容性 | 钉钉电脑端内部应用与电脑浏览器共用前端，均支持钉钉登录；不包含手机布局 | 已确认 P0 |
| 可访问性 | 输入有标签，错误有文字，状态不只靠颜色 | 建议 P1 |
| 性能 | 页面响应、上传上限、队列等待与生成时长阈值待开发计划明确 | 待定 |

## 9. Agent 与外部依赖

| 编号 | 依赖 | 当前情况 |
|---|---|---|
| DEP-001 | Linux 上的 Codex CLI 执行环境和认证 | 部署方向已定，安装及认证未核查 |
| DEP-002 | 壁纸、商品、文字三个 Skill | 暂无；用户要求稍后处理 |
| DEP-003 | Skill 需要的图像处理工具或服务 | 待 Skill 方案明确，不预先承诺能力 |
| DEP-004 | 文件持久存储与可下载输出 | 已确定 MinIO，部署与访问权限需联调 |
| DEP-005 | 钉钉企业内部应用、授权及成员接口 | 双端登录已确定，企业配置与域名尚未联调 |

- 系统异步执行用户提交的生成和修改任务；设计与运营均可审阅、返工和归档。
- 每个新任务拥有独立 CLI 会话；任务上下文包含指定 Skill、相关图片和要求。
- 已确认按业务任务关联会话，不按运营账号共用：首次生成新建会话，返工按已保存会话 ID 续接；后台每轮启动 CLI 进程，完成后退出，保留会话记录。归档由后端完成，不调用 CLI。
- 第一期不实现自动上下文摘要、历史要求整理或自动切换会话。续接失败明确报错、保留旧结果，不自动新建会话冒充续接。仍保留必要的输入、结果版本和会话 ID；工具权限、进程超时、自动重试次数及费用上限留待技术设计。
- 业务任务状态独立于 CLI 进程存活状态；进程退出或文本回复不能单独作为图片完成证据。
- 日志建议记录任务、执行轮次、错误与输出路径，供定位问题；运营页面显示业务进度及结果。
- 效果评测按用户要求推迟，本轮不设效果样例收集门槛。

## 10. 平台功能验收标准

以下已确认需求可作为验收基础；涉及建议默认的条目在对应功能实施前按第 11 节处理，不包含图片视觉效果评分。

- [ ] AC-001：选择三种不同处理类型提交时，任务类型和调用的 Skill 与选定类型一致。
- [ ] AC-002：设计或运营上传保存模板并绑定可用 Skill 后，全员可直接从对应入口选择使用，不存在主管发布审批。
- [ ] AC-003：缺失必需素材或可用 Skill 时，提示原因且不伪造生成成功。
- [ ] AC-004：创建两个新任务时，各自关联独立 CLI 会话，素材和输出互不覆盖。
- [ ] AC-005：执行返回可读取图片后，前端可以预览并下载对应文件。
- [ ] AC-006：对整套提交修改意见时，记录意见、目标范围和返回的新结果。
- [ ] AC-007：仅对第 N 张返工时，其他图片的当前版本及文件内容保持不变。
- [ ] AC-008：归档后，在成品库可找到记录并下载归档图片。
- [ ] AC-009：已确认——关闭页面再进入任务中心，能够查看后台任务最新状态。
- [ ] AC-010：建议默认——重复提交同一次操作只创建一个任务；达到执行并发上限后新任务排队。
- [ ] AC-011：建议默认——单张返工失败保留原版本，其他成功图片仍可查看。
- [ ] AC-012：建议默认——模板或 Skill 更新后，已提交任务保留原版本引用。
- [ ] AC-013：建议默认——后续返工不改变之前已归档的文件。
- [ ] AC-014：建议默认——无权限用户无法通过直接文件地址访问受限资源。

- [ ] AC-015：同一任务首次生成、单张返工、整套返工使用相同 CLI 会话 ID；同一用户第二项新任务使用不同会话 ID。
- [ ] AC-016：首次生成与返工作业尚未完成时，提交已返回受理标识，状态可独立查询；关闭页面后执行继续。

- [ ] AC-017：四角色均能跨创建人查看、删除模板/任务/成品，设计主管可查看全员任务与统计，其他管理权限符合矩阵；在资源归属等条件相同时，设计与运营的允许/拒绝结果完全一致，角色变更或禁用后旧会话失去相应权限。
- [ ] AC-018：调用明细可复算每日统计，缺失 usage 不显示为 0；Worker 失联与 CLI 空闲正确区分，配置修改可审计。
- [ ] AC-019：同一成员通过钉钉桌面端及浏览器登录映射同一系统用户及角色；陌生企业、未授权/禁用成员不能进入业务。
- [ ] AC-020：两端登录过期、拒绝授权、回调刷新/重放、下载上传和异步任务闭环通过实测。

完成定义：P0 范围实现并通过相关平台功能验收，异常和空状态可用，建议默认项完成确认，设计和开发计划已对齐；真实生成联通依赖 Skill 与执行环境就绪。本文完成不等于系统完成或通过容量验收。

## 11. 尚需明确的事项

不阻塞本版 PRD 编写；按依赖阶段处理：

| 编号 | 事项 | 何时需要 |
|---|---|---|
| Q-001 | 格式/10 MiB/20 张及输出张数已确认；真实输出尺寸、格式转换兼容仍待联调 | 真实输出联调前 |
| Q-002 | 模块默认 Skill 与模板专用 Skill 的绑定方式（维护权限已确定仅超级管理员） | 配置功能开发前 |
| Q-003 | 已确认采用上传图片加自然语言要求，任务名称必填、SKU 可选 | 已确认 |
| Q-004 | 回收站形式与保留期限；主管全员任务/统计、模板免审批、全员查看/删除均已确认 | 账号与资源管理开发前 |
| Q-008 | 钉钉企业及内部应用配置、可见范围、接口权限、HTTPS 域名/回调与超级管理员成员标识 | 双端真实登录联调前 |
| Q-005 | CPU/内存/磁盘/带宽，执行并发和峰值任务量 | 容量设计与部署前 |
| Q-006 | CLI 认证、实际处理依赖、固定会话续接的实机验证、超时重试及成本上限 | 真实任务联调前 |
| Q-007 | 下载、归档及历史版本的建议默认规则 | 相应功能开发前 |

2026-09-09 用户已确认：JPG/PNG/WebP，单文件 10 MiB、每组最多 20 张；任务名必填、SKU 可选；文字输入采用图片加自然语言；按实际模板/输入张数输出。

其余可审阅默认值仍未确认：新版本保留、完整套图才整套归档；逻辑删除后 30 天回收且引用保护。运行建议：初始并发 1、单轮硬超时 30 分钟、自动重跑 0；普通列表 P95 ≤ 1 秒（不含上传/下载/生成），每日备份、RPO 24 小时、RTO 4 小时，均需联调和资源验证。是否采用这些业务默认在对应阶段确认，设计与运营始终适用相同规则。

图片效果及 Skill 编写均按用户要求后置，不作为当前文档出稿条件。

## 12. 与业务交付包的关系及下游影响

2026-09-08 开发计划确认：用户要求拆分 DEV-PLAN.md；正式前端直接复用当前已认可原型（`http://localhost:3007/#/image-processing/wallpaper`）对应的 `prototype/source/` 源码。后端明确采用 Python FastAPI，数据库 PostgreSQL，图片存储 MinIO。当前授权为规划和本机 Docker 只读检查，不表示已经启动正式编码或部署。原型及原框架保留作为对照；开工时从源码承接，不能以打包后的 HTML/JS 为开发源。

本机检查发现 PostgreSQL 16.15、MinIO RELEASE.2025-09-07T16-13-09Z 正在其他项目容器运行；仅作为版本与配置参考，不表示已验证目标 Linux 服务器。独立项目资源、队列与执行适配方案见 DEV-PLAN.md。其余未确认业务默认仍按第 11 节管理，不能因技术栈确认而自动视为确认。

2026-09-08 原型交付阶段的已确认约束（历史背景，当前开发顺序见第 13 节）：前端以 `D:/Work_Project/art-design-pro` 为底座，参考运行地址 `http://localhost:3006/#/dashboard/console`。直接复用其布局、导航、主题和既有组件，不另造底板。当时仅交付 HTML 交互原型供评审，不启动正式项目开发；生成、Skill、用户与数据均为演示，不连接 CLI 或服务器。原型交付及其制作素材放在 `prototype/`，保留原框架项目。正式开发需在后续环节单独启动。

保留模板库、后台任务、结果预览的业务方向；新增壁纸和文字处理类型、Skill 绑定、整套/单张意见及成品库。原稿固定 20 张、多角度素材限制、设计专属模板权限和手工区域配置不自动继承。

业务交付包内的原始设计资料可以作为视觉参考，但功能已不完全对应本 PRD，需要后续更新导航、模板配置、结果详情和归档页。当前仅登记影响，不改动原始设计文件。现已完成 Design-Brief.md 与 DEV-PLAN.md；下一步按开发计划承接工程。原业务交付包仅作参考，不覆盖当前 PRD。

## 13. 管理中心、四角色与钉钉登录详细设计

以下统一作为本 PRD 的组成部分维护，已确认需求与建议细则的状态保持不变。

### 已确认与建议边界

已确认四角色：系统超级管理员（用户本人）、设计主管、设计人员、运营人员。Skill 包提交、维护及服务器安装全部归超级管理员；钉钉电脑端企业内部应用与电脑 Web 共用钉钉身份登录。

已确认设计人员、运营人员均使用完整业务流程：模板创建维护、三类图片处理、整套/单张返工、下载、归档与个人调用统计；两者共用同一权限集合和数据范围规则，仅岗位名称不同。设计主管查看全员任务及统计；模板上传保存后直接可用；四角色均可查看和删除全员模板、任务与成品。不新增生成结果的主管审批流。第一期不做复杂上下文管理，新业务任务新会话，同任务返工续接，所有执行走异步队列。

### 前端依据与导航

以 `prototype/source/` 当前已认可原型为源码基础，保留 art-design-pro 布局、侧栏、标签页及组件。使用现有 ArtTable、统计卡片、图表、Element Plus 表单和状态标签，新页面无需重新制作底板。原来的图片处理/任务中心/模板库/成品库保持业务职责。

```text
图片处理
  替换壁纸 / 替换商品 / 替换文字
任务中心
模板库
成品库
管理中心（按权限展示）
  调用统计
  执行监控
  用户与角色
  Skill 管理
  系统配置
```

设计人员和运营的调用统计仍采用个人视图；设计主管和超级管理员可查看全员调用统计。所有角色均可查看全员模板、任务与成品；第一期不需要为这些资源配置团队查看或删除范围。

### 权限矩阵

下表区分已确认权限与仍沿用的操作建议。全员查看/删除只针对模板、任务和成品，不扩展为全员维护 Skill、系统配置或查看全员调用统计；编辑他人模板、替他人返工等操作不由查看/删除权限推导。

| 功能 | 系统超级管理员 | 设计主管 | 设计人员 | 运营人员 |
|---|---|---|---|---|
| 创建三类处理任务 | 可 | 可 | 可 | 可 |
| 查看模板、任务、成品 | 全部 | 全部 | 全部 | 全部 |
| 删除模板、任务、成品 | 全部 | 全部 | 全部 | 全部 |
| 返工任务（操作范围建议） | 全部 | 全部 | 自己 | 自己 |
| 创建模板 | 可 | 可 | 可 | 可 |
| 编辑/停用模板（操作范围建议） | 全部 | 全部 | 自己 | 自己 |
| 模板上传保存后使用 | 直接可用 | 直接可用 | 直接可用 | 直接可用 |
| 绑定已发布 Skill | 可 | 可 | 可 | 可 |
| 上传/安装/更新/启停 Skill | 可 | 不可 | 不可 | 不可 |
| 成品浏览与下载 | 全部 | 全部 | 全部 | 全部 |
| 归档（操作范围建议） | 全部 | 全部 | 自己任务 | 自己任务 |
| 调用统计 | 全员 | 全员 | 个人 | 个人 |
| 队列及业务异常 | 全局 | 全员任务 | 全员任务 | 全员任务 |
| CLI、依赖健康详情 | 可 | 只读总体可用状态 | 简要不可用提示 | 简要不可用提示 |
| 分配角色、禁用用户 | 可 | 不可 | 不可 | 不可 |
| 系统参数与钉钉配置 | 可 | 不可 | 不可 | 不可 |

角色是系统权限，不等同钉钉企业管理员身份。第一期每人一个业务角色，超级管理员为独立最高权限；未分配用户保持待授权，不默认成为运营。角色与资源范围由后端校验，URL、API 和签名下载都不能绕过。禁用或降权后现有系统会话在下一请求重新校验。操作留存实际操作者，代他人返工不混淆调用归属。

### 管理中心页面设计

#### 调用统计

**调用统计建议口径**：

- 按 Asia/Shanghai 自然日统计，支持单日及日期范围；新建任务按创建时间归日，执行按实际开始时间归日。
- 显示新建任务数、首次生成/单张返工/整套返工执行次数、执行成功/部分失败/失败/超时、成功输出图片数、排队时间和执行耗时。一个任务初次生成后返工两次计 1 项任务、3 次实际执行，不等于 3 次模型内部请求。
- 一次实际 CLI 启动记一次 execution attempt；消息重复投递未启动 CLI 不增加执行次数，真实重新启动另记 attempt。记录任务创建人和本轮操作者，全员调用统计按实际操作者归属，避免管理员代操作算给运营。
- 队列中未启动的不计实际调用；跨日未完成执行显示进行中，完成后更新所属开始日结果。成功率为成功次数 / 已结束执行次数，部分失败单独展示且计入分母，未结束不入分母；零分母显示无数据。输出图片数按成功持久化的版本计数，返工新版本也计入并标明口径。
- Token 仅展示 CLI 可靠返回的实际 usage，字段缺失显示“未提供”，不计为 0；同轮累计事件不得重复相加。预检失败可能无 usage，不能推断免费。费用和账号剩余额度不从 Token 猜算，第一期不承诺账单或配额看板。

顶部显示日期范围、用户、处理类型筛选；个人角色不出现全员选择器。中部展示新建任务、执行次数、成功/失败、成功产出图片及排队/执行耗时；下方按人员列日汇总，点击展开调用明细和关联任务。

#### 执行监控

超级管理员看到 Worker 心跳/最后检查时间、队列数、运行数、并发上限、CLI 版本与配置就绪情况、最近调用成功或错误，以及 PG/Redis/MinIO、临时目录空间。表格列运行任务、操作者、会话标识、阶段、已运行时间和脱敏错误。

CLI 每轮结束退出是正常行为，空闲不报离线。Worker 失联、依赖不可达、认证拒绝、限流、超时分别表达；历史成功不能代替当前健康检查。过期检查结果显示未知。健康检查不周期性发起生成或消耗模型调用。

设计主管看到全员队列与失败任务、总体服务可用性，不暴露服务器路径、凭据和原始日志。普通角色可在任务中心查看全员任务及业务状态。

#### 用户与角色

从已验证的钉钉成员建立用户，显示姓名、部门、角色、状态及最近登录时间。超级管理员分配四角色，启停系统账号；不提供自助提权。初始超级管理员通过部署配置指定企业成员标识，不采用“首个登录者就是管理员”。

建议第一期允许目标企业成员首次登录生成待授权记录，等待超级管理员分配角色；首次入职不会自动获得业务权限。企业成员资格在登录时校验；本地禁用即时生效。离职成员检测需在接入通讯录权限后通过定期校验或事件同步撤销会话，建议目标在成员变更后 15 分钟内生效，并在联调验证权限与接口频率。

#### Skill 管理

仅超级管理员进入。显示三种处理类型的当前 Skill 版本、包校验和、安装节点、安装状态及最近变更。提供上传、校验、安装、设为可用、停用和版本切换；不让设计/运营上传可执行包或改服务器环境。

包可持久化到 MinIO，但“包已上传”不等于“服务器已安装”。安装由受控后台作业异步完成：校验包 → 准备版本目录 → 检查依赖与可读性 → 成功后发布。失败保留旧可用版本。队列中的任务冻结 Skill 版本，在运行任务不受更新影响；被任务引用的版本不能直接删除。首期单 Linux Worker，页面如实显示安装状态，不做无依据的集群管理界面。

#### 系统配置

仅超级管理员修改执行并发、执行超时、上传限制和默认 Skill 绑定。所有变更有校验和审计，仅影响后续轮次。钉钉区域展示企业/App 标识、回调域名和配置检测结果；AppSecret、CLI 凭据保留部署环境，不返回前端。第一期不提供网页任意终端。

### 钉钉双端登录

一套 Vue 前端、一套 FastAPI、一套用户与权限表，设计为钉钉企业内部网页/H5 应用。不是开发两套产品，也不是小程序重写。

| 入口 | 登录体验 | 后端处理 |
|---|---|---|
| 钉钉电脑端工作台 | 打开应用，容器获取临时免登授权码 | 验证授权码，核实企业成员及应用可见范围，建立系统会话 |
| 普通电脑浏览器 | 点击钉钉登录，进入官方网页授权/扫码确认流程 | 验证回调及 state，获取可信身份、验证企业关系，建立同一系统用户的会话 |

浏览器能用钉钉身份登录不等于一定静默免登；扫码/授权是否需要确认由钉钉登录状态和授权流程决定。钉钉客户端内部免登失败时给出重试和说明，不静默套用演示身份。

账号映射采用后端验证的企业身份（目标 corpId 与成员 userId），Web 返回标识通过官方接口映射到目标企业成员；unionId 仅作经过验证的关联依据。不同入口返回的标识不能直接当作相同值，不以姓名或手机号猜测合并。

使用对应企业网页应用的 SDK/授权流程；不误用小程序 getAuthCode。钉钉官方 JSAPI Explorer 展示 requestAuthCode 微应用 PC 支持，但具体 SDK 版本、调用参数和码交换链路在接入阶段按企业应用类型固定并实测。授权码不持久化为登录凭据；AppSecret 和钉钉访问令牌仅服务端持有。

网页回调使用后端固定 HTTPS 路径 `/api/v1/auth/dingtalk/callback`，不使用 Vue hash 片段作为 OAuth 回调。验证短期、一次性 state 及浏览器绑定，避免回调重放和任意跳转；完成后跳转允许的站内页面。系统会话采用安全 Cookie，并实测钉钉桌面容器 Cookie 行为。用户关闭钉钉窗口不会取消已受理的异步任务。

#### 接入前需要的环境资料

- 目标企业 CorpId、企业内部应用 Client ID/AppKey、AgentId、应用可见人员范围。
- 服务端安全配置 AppSecret；按所选登录及成员查询流程申请最小必要权限。
- 生产 HTTPS 域名、PC 应用首页、网页授权回调及相关域名配置；不能把 localhost:3007 当正式钉钉应用地址。
- 超级管理员本人钉钉企业成员标识；主管查看全员，无需提供团队范围。
- 实际 Windows/Mac 钉钉版本与目标浏览器，用于确定验收矩阵。以上配置在联调前提供，不阻塞文档设计。

#### 双端验收

钉钉 PC 与 Chrome/Edge 都覆盖首次登录、再次访问、过期重登、拒绝授权、未授权成员、禁用角色、同一身份映射、上传、预览、单图/ZIP 下载、异步提交、返工及归档。重点实测容器中的下载打开方式、窗口大小、Cookie 与外部授权回跳；不能只测浏览器后宣称钉钉兼容。Mac 若有业务用户则纳入实机矩阵。手机适配不在本期。

### 开发接入位置

已确认开发优先级：先完成全部前端，再开始后端开发。Phase 1–4 承接现有 art-design-pro，完成三种图片处理、模板库、任务与返工、成品库、管理中心及登录状态页面；使用独立模拟接口体验完整交互，不依赖 FastAPI、数据库、MinIO、CLI 或钉钉配置。每完成一组页面即提供预览，全部前端完成后再进入 Phase 5 后端工程。

Phase 5–11 完成后端基础、文件与模板、异步队列、Codex 执行、同会话返工和归档，并逐模块接入已完成前端；Phase 12 接钉钉真实登录与四角色授权，Phase 13 接管理中心真实统计和监控，Phase 14 完成双端联调部署。登录页面先完成，真实认证仍在后期接入。

前端完成指页面、交互和各状态在模拟接口下通过验收，不代表真实生成、持久化、授权或监控已完成。前端先定义数据类型和接口契约，模拟与真实接口共享契约；模拟环境明确标识，生产禁止模拟模式。后端从文件与任务阶段记录稳定用户 ID、资源所有者和每轮操作者；本地联调使用服务端固定测试身份，生产禁止开发身份，真实接入时不自动将测试数据归入钉钉成员。具体交付和验收见 DEV-PLAN.md。

### 官方参考

- [钉钉企业 H5 应用免登示例](https://github.com/open-dingtalk/h5app-auth-demo)：JSAPI 授权码、服务端用户识别及 PC 工作台应用配置。
- [钉钉微应用 requestAuthCode 支持表](https://open.dingtalk.com/tools/explorer/jsapi?id=11723)：微应用 PC 免登能力，实施时核对 SDK 和客户端版本。
- [钉钉网页登录教程](https://open.dingtalk.com/document/orgapp/tutorial-obtaining-user-personal-information)：浏览器登录入口；页面为动态文档，本轮正文抓取有限，具体接口参数须在联调前再次验证。
- [钉钉官方教程目录](https://open.dingtalk.com/tutorial/)：区分内部免登与第三方网站扫码/账号授权场景。
