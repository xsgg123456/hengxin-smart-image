# Agent Harness框架 · Codex 版 安装说明

1. 把 AGENTS.md 放进你的项目根目录。
2. 把本包里的 agents 文件夹重命名为 .agents（前面加一个点），放进项目根目录。
3. 把本包里的 codex 文件夹重命名为 .codex，同样放进项目根目录。
4. 重命名后文件夹会变成隐藏状态，这是正常的。macOS 在访达里按 Cmd+Shift+. 可以显示隐藏文件。
5. 在项目根目录启动 Codex 即可，主控文件是 AGENTS.md。
