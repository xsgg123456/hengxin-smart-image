# 恒鑫智图 · 设计规范与原型说明

## 2026-09-22 · API 换套图预览

沿用当前前端侧栏、页头、页签、蓝色主题、hx-page / hx-create-grid、art-card、现有图片查看器及 Element Plus 表格/抽屉。独立 API 换套图菜单下为新建换图和换图记录；输入图片明确标注原图序号与末张共用素材。演示控制与模拟结果明确标记。具体范围见 [API 交互预览计划](docs/API-IMAGE-PREVIEW-PLAN.md)。

## 品牌标识定稿方向 · 2026-09-14

用户已确认采用「恒鑫智图」概念方案。所属公司「前海恒鑫」，用途副标题「京东业务生图」。已确认概念稿归档于 `hengxin-smart-image/branding/approved-concept.png`，矢量资源和用法见同目录 README.md。

- 图形：保留红色 H 形折面结构和右上单个橙色菱形。矢量版收整边缘、统一间隙；24px 及以下使用同轮廓的简化纯色版，避免细碎折面。不得拉伸或随主题改成蓝色。
- 颜色：品牌红 #F1362F、折面深红 #D92D27、像素橙 #FF9A16；浅底字标 #20242D，深底使用反白版本。全系统蓝色操作主题继续沿用原框架。
- 字标：采用思源黑体 Medium 的清晰结构收整概念稿中过厚的笔画；交付横版 SVG 将文字转为路径，避免缺字和字体替换。副标题仅用于有足够空间的完整横版。
- 应用：侧栏/页头继续复用 ArtLogo 与既有系统名称位置；登录页沿用原布局，用完整横版展示名称和用途；浏览器及应用入口使用独立图标。
- 交付：完整横版、紧凑横版、彩色/反白独立图标、透明 PNG、favicon ICO、应用 PNG，以及可直接查看的预览页。保持安全留白，不让字标在窄面板中挤压变形。

以下页面原型说明基线：2026-09-10（当次仅同步进度）；品牌规范已于 2026-09-14 按上节更新。页面基线依据 Product-Spec.md v0.17 及用户指定 art-design-pro 前端底座。

开发承接确认：用户已认可当前视觉方向并要求以其拆分开发计划。正式开发已经将页面、组件、主题和导航承接到 `hengxin-smart-image/frontend/`，以正式前端作为视觉验收基准；新增登录及管理能力也遵循现有 Art 组件先例。以下原型范围说明保留用于描述历史交付状态，正式工程承接与验收记录见 DEV-PLAN.md。

原型交付曾包含静态 HTML/JS/CSS 演示；旧原型目录已清理。正式工程位于 `hengxin-smart-image/frontend/`，当前阶段进度以 [DEV-PLAN.md](DEV-PLAN.md) 为准。文件、模板、任务、CLI 执行、返工及归档已分阶段接入正式工程。真实 CLI 需专用 Linux Worker，真实业务 Skill 效果与生产联调不由 Demo 或模拟测试证明。

开发顺序：先完成全部前端（业务页面、管理中心、四角色视图和登录状态），逐组提供预览，使用明确标识的模拟接口；前端验收完成后再开发后端并逐模块接入。真实钉钉登录仍后置，最后做双端实机验收。前端交互完成不代表真实生成、存储或鉴权完成。

## 设计原则与复用

2026-09-21 Skill轻量目录管理：替代下方版本分组界面。使用既有ArtTable/ElCard/ElTag/ElButton，Skill一行展示名称、完整描述的可展开摘要、处理类型、状态和默认标记；不显示版本号或展开版本。页面主操作“同步 Skill”，加载和异步同步状态可见；异常可读，未识别类型时提供选择。移除登记使用既有危险操作确认，说明保留服务器文件和历史任务。模板选择同步展示描述；任务摘要只移除Skill版本显示，保留模板版本。

2026-09-17 Skill 本地版本登记：继承 Skill 分组表、ElDialog 表单、ElAlert、状态标签和危险操作确认；“登记版本”替代上传，“检查部署”替代本地版本安装。展示本地/历史 ZIP 来源、待部署/检查中/已验证未启用/检查失败，不把文件存在或检查通过表述为图片效果验收。移除登记与主操作分离，说明不会删除服务器文件。表单不出现 ZIP 选择器、任意路径或终端输入；技术校验值沿用次级信息层级。

2026-09-14 路由体验修复：沿用任务抽屉、成品弹窗与现有 ElButton/ElDropdown/ElAlert；URL 同步详情对象及列表条件，归档反馈提供查看成品、成品提供来源任务入口，新建任务用三类选项。无权限显示已有 403 页面；来源任务不可用在成品弹窗内说明，保留归档下载。此轮不调整五个一级菜单及三种图片处理页面。

直接复用 art-design-pro 的 Vue 3、Element Plus、ArtSidebarMenu、ArtHeaderBar、ArtPageContent、路由和主题，业务页面沿用浅色卡片、蓝色主操作、圆角及系统字体。不采用其他设计工具重新绘制框架。用户已明确要求 HTML 可交互交付，因此直接实现浏览器原型。

主题沿用框架主色 #5D87FF、灰阶、白色内容卡片及浅灰背景；内容以商品图为主，表单和说明为辅。基础控件使用 Element Plus，侧栏和工作标签使用原有 Art 组件。

## 已交付原型的交付步骤与完成标准

1. 建立原框架副本及原型运行入口。完成标准：保留来源许可，不修改参考仓库，真实加载原有布局与组件。
2. 图片处理一级菜单下的三个生成页面。完成标准：模板选择、素材上传/移除、文字要求输入和演示提交可操作，有空输入校验。
3. 模板库、任务中心、结果详情和成品库。完成标准：创建/编辑模板、搜索筛选、修改意见、单张/整套重试、下载、归档闭环可操作。
4. 编译、浏览器交互和截图自检。完成标准：页面可访问，主流程与错误/空状态可复现；明确演示生成边界。

## 正式前端页面清单

2026-09-09 更新：管理中心新增调用统计、执行监控、用户与角色、Skill 管理、系统配置，详见 Product-Spec.md 第 13 节。四角色为超级管理员、设计主管、设计、运营；设计与运营使用相同菜单、操作入口和数据范围规则，均为生产使用者；Skill 维护仅超级管理员。继承原有 Art 组件，主管和超管显示全员统计，设计与运营显示个人统计；所有角色可查看/删除全员模板、任务和成品，编辑全员模板、返工及归档全员任务；模板保存即用、无主管发布流程。钉钉 PC 容器与电脑浏览器共用页面，新增钉钉登录、授权中/失败/待授权状态，验证容器内上传下载。这些页面已在正式前端 Phase 4 完成模拟交互和角色视图；以下 Phase 4 描述保留历史交付状态；真实认证及管理接口后续已接入，完整真实角色联调仍待验收。

最新导航确认：一个一级菜单“图片处理”，下面三个二级菜单“替换壁纸”“替换商品”“替换文字”。恢复各自独立页面，不采用页面内切换标签；复用原 ArtSidebarMenu 的嵌套菜单。验收：父菜单可展开收起、三个子菜单可访问且正确高亮、模板跳转正确。


| 页面 | PRD 关联 | 主要布局和状态 |
|---|---|---|
| 替换壁纸 | REQ-001/003 | 模板选择、素材区、右侧任务摘要；选择、上传、校验、提交 |
| 替换商品 | REQ-001/003 | 同一框架，独立模板和素材提示 |
| 替换文字 | REQ-001/003 | 图片上传与修改要求；无复杂字体编辑工具 |
| 模板库/编辑弹窗 | REQ-002 | 图片卡片、类型筛选；新建、保存、空状态、移除 |
| 任务中心 | REQ-004 | 全员任务、删除入口、状态筛选、表格、进度；排队、执行、失败、待查看 |
| 结果详情 | REQ-005 | 图片网格、单图大预览、反馈弹窗与修改记录 |
| 成品库 | REQ-006 | 搜索、类型筛选、成品卡片、预览下载与移除 |
| 钉钉登录 | REQ-008 | 容器免登/网页授权；验证中、待授权、失败、成功 |
| 调用统计 | REQ-007 | 统计卡片、日期/用户筛选、明细表；个人/全员范围 |
| 执行监控 | REQ-007 | 心跳与状态卡、任务表；空闲、执行、异常、未知 |
| 用户与角色 | REQ-007/008 | 成员表与授权表单；未授权、启用、停用 |
| Skill 管理 | REQ-002/007 | Skill 目录、描述、同步与可用状态；仅超级管理员 |
| 系统配置 | REQ-007 | 参数表单、校验和变更记录；仅超级管理员 |

原型仅体现 PRD 中的建议默认方案供用户评审，不把演示行为作为已批准的业务规则。未接真实 Skill；生成以明确标记的示例图片模拟。下载真实示例文件，上传的素材仅用于浏览器内预览。演示任务状态保存在浏览器本地，生产权限及后端处理不在此次交付中。

Phase 2（2026-09-09）：沿用现有卡片与表单布局；素材区补充逐图状态、移除/前后排序及失败重试，模板卡片显示版本；列表分页、搜索排序和错误提示复用 Element Plus。仅模拟模式提供预览场景选择，切换重置内存数据，真实模式不显示此工具。

Phase 3：任务继续使用 ArtTable 和现有抽屉/卡片，结果按固定槽位显示。失败/部分失败提示保留旧结果，版本选择复用 ElSelect，轮次记录复用 ElCollapse；危险删除保留确认，下载和归档显示处理中与失败提示，成品卡片沿用现有三列网格。

Phase 4：五个管理页面沿用任务页的 hx-page、hx-heading、hx-stats、ArtTable 和 ElCard；筛选及编辑复用 Element Plus。登录沿用现有 Art 登录布局，移除正式产品中的密码及注册入口。模拟角色/登录场景仅在模拟模式可见，切换刷新并清理旧身份视图；空闲与未知分别显示，缺失 usage 用“未提供”，不显示虚构额度或费用。

后端并发状态适配（Phase 8–10 已实现，保留设计要求）：沿用现有任务详情、按钮与错误提示组件，根据服务端操作资格禁用返工/重试并显示原因；冲突、状态待核实或网络响应不确定时保留用户意见，不自动换请求键重新提交。会话、进程、锁和租约等实现细节不进入业务页面，规则依据 Product-Spec 第 9.1–9.2 节。

Phase7：沿用模板编辑ElDialog、图片排序组件、ArtTable及Skill管理。清空专用选择表示使用模块默认，并明确解析版本；历史版本用只读对话框，模块默认绑定在Skill管理页仅超管维护。


## 2026-09-21 · 交互与视觉扩展（仅本地框架 Demo）

用户要求将图片优化原型拓展到 Demo + 假数据级别。范围、拆分、边界和验收以 [框架 Demo 计划](docs/FRAMEWORK-DEMO-PLAN.md) 为准。沿用当前品牌和正式前端，新增持久化假数据、模板维护、三类模拟任务、版本返工、归档与管理演示。正式工程和既有阶段验收不因本次 Demo 交付改变。


## 2026-09-21 · 用户确认基于现有框架迁移 Demo

用户确认采用现有 Vue 3、Element Plus、Art Design Pro 框架内的独立Demo模式，复用真实布局和业务组件，假数据隔离于生产API。此决定替代上一版独立HTML作为后续交互评审实现方式；旧原型已清理。范围及验收见 [框架Demo计划](docs/FRAMEWORK-DEMO-PLAN.md)。不改变生产业务接口和既有阶段验收结论。

## 2026-09-22 · 正式前端 UI 承接

用户已确认当前框架内的 UI 改进用于正式前端。共享组件同时服务 Demo 与真实 API，Demo 数据与控制入口仍独立隔离。

- 模板、素材、结果、成品使用完整等比缩略图与整组大图预览；支持左右按钮/方向键及关闭返回。看图与模板选择分离，当前模板显示“✓ 当前使用”，更换提供取消。
- 非排序素材上传空态保留大拖拽框，有图时为 104px 网格及追加入口；超过两行可展开，上传中和失败项不隐藏。模板排序与返工圈注入口沿用原行为。
- 未出图且排队/执行中的结果槽位使用轻呼吸/流光，保留真实状态，不模拟百分比；失败及终态停止，遵循减少动态偏好并在离屏/隐藏页暂停。
- 任务详情以结果与恢复操作为先，模板参考图取任务快照；管理页与文字层级继续继承 Art/Element Plus。真实业务和双端验收仍按 DEV-PLAN 的既有范围执行。
