# 单张返工：所见版本与圈注截图验证 · 2026-09-21

对应 REQ-005、AC-007A/B，以下记录本机实现与验证。本轮不修改已安装业务 Skill。后续已获授权发布，线上凭据见 [生产发布记录](SINGLE-REVISION-DEPLOYMENT.md)。

## 已验证

- 前端全套 `node --import tsx --test tests/*.test.ts`：114通过、0跳过；`vue-tsc --noEmit`通过；Vite正式构建通过（41.77秒）。保留既有登录组件同时静态/动态导入提示。原始测试与构建输出在忽略目录 `output/single-revision/`。
- 后端无独立数据库时全套：742通过、148条件跳过，`compileall app migrations`通过；覆盖历史V1→V3、同会话、冻结重试/幂等、跨任务/槽位拒绝、圈注身份/状态/类型/大小、材料真实字节与提示词。原始摘要 `output/backend-single-revision-validation.md`。
- 后端独立PostgreSQL 17全套：785通过、105条件跳过（Linux等环境条件），82.72秒；迁移0013及圈注清理引用保护专项2通过。使用独立临时本机容器与测试账号，完成后已移除容器；日志 `backend-pg-tests-final.txt`、`backend-pg-specific.txt`。首遍测试因Windows临时路径过长失败1项，缩短测试目录后全部重验通过。
- CUA真实浏览器、本地mock服务 `127.0.0.1:3010`：无截图文字提交成功；损坏PNG显示解码错误且禁止提交；移除后可恢复；有效截图上传与替换后仅保留一张；有V2时选择V1，弹窗明确“本次基于V1修改”，带截图提交生成V3。下拉保留V1/V2历史与V3当前，其他七张仍V1；修改记录展示基础版本及截图。1280×720弹窗内滚动，操作按钮可见。
- 浏览器测试暴露并修复草稿复制数组导致上传组件回传误重置的问题，增加数组身份回归断言；前端全套/类型/构建在修复后重验。
- 独立审查指出的整套弹窗不适用截图示例、mock未拒绝已有成品却显式null基础版本已修正；增加422回归断言并通过全套测试。

## 实现边界与发布要求

- 浏览器使用mock，验证交互、版本和快照链路，不代表真实模型生图效果；后端测试使用fixture/隔离材料，不发起付费模型任务。
- 部署时先运行新增迁移0013，再更新API/Worker/前端；迁移新增两项可空引用。已有轮次兼容，旧客户端省略基础版本时取当前版本并冻结。
- 返工继续同一任务的既有Codex会话，但每次提交是新的执行轮次。重试使用失败轮次已冻结输入；整套修改保持各位置当前结果。
- 本机验收时未部署生产、未提交Git；后续生产发布另见上述记录。独立两阶段审查结果见 `SINGLE-REVISION-REVIEW.md`。
