# Phase 13.2 / 13.3 本机验证 · 2026-09-16

## 范围

分支 `codex/management-monitor-settings`，基于 `3baa541`。仅本机开发：未提交、推送、创建 PR 或部署 VPS；VPS 仍为业务代码 `d2e5076`、迁移 `0010`。

新增 `/management/monitor` 真实监控，以及 `/management/settings` 版本化配置和审计。迁移 `0011` 新建配置/审计表。配置影响新轮次；旧轮次保留提交时的超时及版本。并发上限受部署容量约束，不在线扩容 Worker。钉钉字段只读，无凭据回传。

## 自动验证

- 独立 PostgreSQL 测试库验证 `upgrade head → downgrade 0010 → upgrade head`，测试结束删除临时数据库，未对业务数据库执行测试用例。
- 首轮集成后端：628 passed、95 skipped。跳过项不算通过，包含当前运行环境不满足的 Linux 执行验证；未调用真实生图模型。
- Node 24.18.1 从官方分发下载并核对 SHA256 后使用，首轮前端 97 tests、类型检查及 Vite build 通过；构建有既有包体积提示。
- 配置用例覆盖四角色权限、越界、持久化与无变更保存、版本409、默认 Skill 入口共享版本、原子回滚、上传实际限制、新轮次冻结；PostgreSQL 两连接竞争只有一方成功。
- 监控用例覆盖无心跳/过期/未来心跳、真实空闲、依赖异常、主管/普通成员权限、Worker 磁盘/版本采样及不调用模型。
- 独立审查发现并修复的问题与最终证据，以[审查报告](../../docs/reviews/phase13-management-local-review.md)及本文后续记录为准；上述首轮结果不替代最终候选验收。

## 本机实际运行

- 重建本机 backend:phase8 镜像；旧镜像保留为 `before-phase13-management-20260916`。确认无活动任务后经现有控制器停止、迁移、启动，保留数据库和素材卷。
- API 8008 的 settings、monitor 均返回200；真实前端3008通过HTTP加载，未使用mock。
- Worker `hx-dev-wsl` 新鲜心跳、CLI 0.153.4、容量2；数据库/Redis/MinIO正常，队列和运行数均0，页面显示空闲。
- 浏览器检查配置字段、默认Skill、只读钉钉与空审计，监控真实环境和任务列表。点击历史任务打开正确详情，未重试、修改或发起生图。
- 本机钉钉未配置，不把本机开发身份视作真实钉钉登录验收。管理员配置保存/冲突/权限的验证使用隔离测试数据库，不改用户当前业务配置。

## 最终候选回归

候选 `e5883dd7abd2cc03aaceab28b685fba32d1edb26edb3fedfc9328ad03bd8d74b`（复审总数一致性修复后）：

- 后端最终独立 PostgreSQL 迁移升降级与全量回归：**639 passed、95 skipped**，62.41秒。
- Node **24.18.1**：**100 tests passed**、类型检查通过、最终 Vite build 通过（32.27秒）。
- 修复运行任务被截断、结构化错误降级未知、查询失败显示0和部署超时边界不一致。101条额外任务测试覆盖全部运行状态保留、总数/截断提示；总数与数据候选在同一条 SQL 快照内计算，避免排队转运行时跨查询漏项。
- 审查结论以同一候选的两阶段独立报告为准；真实生图、真实钉钉与 VPS 不在本次新增验收范围。
- 最终本机镜像再次启动成功；真实 PostgreSQL 查询返回 `taskCount=13`、列表13条、队列0、运行0、Worker空闲。历史错误已显示“本轮所有图片均处理失败”或“输出清单缺失或不符合约定”，不再丢失已有固定诊断。

## 交接边界

后续接手应包含已提交的业务源码、迁移、测试及文档；不要加入 output 原始业务日志、.env、凭据。`.codegraph` 是本机索引。真实角色与钉钉双端联调、三类 Skill、容量与恢复验收仍按 DEV-PLAN 后续安排。

最终独立审查：同候选 Stage 1 PASS、Stage 2 PASS。reviewer 使用自己的本地浏览器标签亲自比较 monitor/settings/usage 页面；前期浏览器连接阻断已解决，详见审查报告最新结论。
