# 任务素材与 Codex 执行播报 · 2026-09-11

## 范围与验收

对应 Product-Spec「生图过程可观测补充」。任务详情展示提交时冻结的 sources 缩略图、名称、数量与原图预览；空态、加载态和图片失败态有提示。返工继续展示同一套原始输入。

仅采集 CLI `item.completed / agent_message`。排除 reasoning、工具原文和命令原文；限制消息长度，移除链接目标、路径、代码及技术清单语句，凭证关键词出现时整条舍弃。按纯文本渲染，Codex 播报不改变平台校验或发布状态。此功能是面向用户的执行播报，不提供模型思维链。

最近三条默认可见，可展开全部。系统事件单独折叠，相邻相同阶段、相同内容合并展示；切换任务、轮次或身份重置展开状态。实时采集使用接收时间；历史 JSONL 无逐条时间字段，只能标记“历史输出，未记录时间”。保留最多100条事件的已有上限，展示数量表示已保留数量。

## 本轮验证

- WSL Python `pytest -q`：576 passed、39 skipped（20.53 秒）；包含事件选择、脱敏、重复去除、真实API DTO的 null 时间兼容、授权和 claim fencing 回归。审查发现的 Unicode/数字路径（包括纯数字根目录）与文本清理后凭证关键词拼接问题已补回归并修复，最终公开文本再次检查凭证标记。
- 固定 Node 24.18.1、pnpm 10.33.4：typecheck 通过，test 79 passed，build 成功；已有构建包大小和动态导入提示保留。
- 后端镜像构建成功；使用既有本地控制器检查空闲并更新 API/WSL Worker，不重置数据库、任务或文件。
- 对开发任务 b6db609f-c84a-486d-94a8-38cfc2f4c13e 的两份已有真实 CLI 日志提取播报，各4条，追加到对应 attempt；时间为空，平台事件、终态与图片版本保持不变。一次性脚本位于被忽略的 output/playwright/local-codex/backfill_public_messages.py；不将它作为自动生产迁移。
- 浏览器真实任务验证：原素材 4.png 可打开完整大图；当前轮次默认3条，展开4条；切换历史轮次自动收起并显示该轮4条记录，含“第1张已生成”和“四个小窗”处理说明，未混入当前4K返工记录。

本轮未再次付费调用模型生图；实时采集链路由事件文件→Observer→真实测试API验证，历史内容由既有真实CLI日志与浏览器验证。其他 Phase 11A 待办和跳过项不因此视为通过。
