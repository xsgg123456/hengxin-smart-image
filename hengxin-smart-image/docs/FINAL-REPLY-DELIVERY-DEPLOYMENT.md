# 最终回复收图生产发布 · 2026-09-17

按用户授权已发布至 https://zhitu.qhhengxin.top/ 。新任务使用简洁业务提示词和 final-reply-v1 收图协议，从本次最终回复明确交付的图片收取成品；允许修复后的图片，保留任务隔离、数量与文件有效性检查。历史执行恢复继续识别旧协议。

## 发布凭据

- 发布标识：`final-reply-20260917-59d22f5`。
- 两阶段批准快照：`59d22f5a86e4d5cde79d48205c0c2765552296f38fdb2a231c7588b30b3304bb`。
- Git 基线：`c6071640d544788184317189a61188379b03047d`；本次发布包含经批准、尚未提交的工作区代码，快照标识不是 Git 提交号。
- 发布包 SHA256：`a260eb91f4d8f4057bd2a3192552de715ecd13988bbc30625b60e683509138ce`，共531个部署文件。
- 镜像：`hengxin-smart-image-backend:final-reply-20260917-59d22f5`。
- 发布记录、脚本及日志：`/opt/hengxin-releases/final-reply-20260917-59d22f5/`，`deploy.exit=0`，日志结束为 `DEPLOY_COMPLETE`。
- 备份：`/opt/hengxin-backups/final-reply-20260917-59d22f5/`，含数据库、旧应用及配置。切换前两次确认无在途任务，迁移保持0012。

## 上线核验

- 原生代码及前端531个部署文件哈希匹配；API和Outbox容器各118个后端文件哈希匹配。
- Worker、API、Outbox和Web启动成功；公开就绪探针返回 PostgreSQL、Redis、MinIO 全部 up，首页 HTTP 200，匿名身份接口 HTTP 401。
- 浏览器打开生产网站，显示恒鑫智图登录页及钉钉登录入口。
- 隔离环境只读查询确认 `gpt-6-astra` / `high`，模型支持该推理强度；没有触发新的模型生成。
- 两个业务 Skill 内容哈希未变，真实隔离读取探针通过：普通版 `jd-main-image-wallpaper-camera-swap/1.0.2` 仍为默认；优化版 `jd-main-image-wallpaper-camera-swap-it-optimized/1.0.0` 可绑定。
- 使用已安装的生产收图代码读取之前成功实验的实际最终回复及图片，收取四张800×800成品，字节哈希与原成品一致。简洁提示词检查通过。

## 验收边界与回滚

本次未创建付费换图任务、未重跑历史失败任务。上述真实产物回放证明新收图器能接收既有成品，不替代部署后的网页新任务完整生成验收。下一步由网页新建一轮，核对实际提示词、输出和下载结果。

依赖审计 critical=0，仍有 high62 / moderate39 / low5 告警，本次未升级依赖。实现测试及独立审查见 [验证记录](FINAL-REPLY-DELIVERY-VALIDATION.md)。

回滚先停止新任务接纳并确认无在途执行，再按发布脚本中的恢复逻辑恢复备份应用和配置、移除新版本独有代码文件，重启旧镜像与原生Worker并检查健康。数据库迁移未变化，不应为代码回滚覆盖上线后的业务数据；数据库备份仅在确有恢复需要时使用。
