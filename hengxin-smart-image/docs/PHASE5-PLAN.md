# Phase 5 执行计划

范围依据：DEV-PLAN Phase 5、Product-Spec v0.14。保持 Phase 1–4 页面；真实身份、文件业务接口与 AI 执行属于后续阶段。

1. 后端基础：FastAPI /api/v1、配置校验、统一错误、健康探针、SQLAlchemy/Alembic；验收为锁定依赖安装、迁移及依赖健康通过。
2. 通用队列：同事务写测试作业与 outbox，独立派发器及 Celery Worker，PG 保护重复消息；验收为 Linux 容器内真实派发、重投和 Redis 故障恢复。测试入口默认关闭，仅开发/测试显式开启，禁止生产开启；测试作业不是 AI 能力。
3. 环境与契约：独立 Compose 项目、新命名卷、镜像 digest、OpenAPI 与现有前端类型核对；验收为不挂载其他项目卷、分页/错误/幂等结构测试通过。未实现业务接口明确返回未实现，不返回模拟业务数据。
4. 收尾：前端测试与构建、3007/3008 回归、后端集成测试、独立两阶段代码审查；全部通过后记录证据并中文提交。

文件所有权：后端基础及队列由 backend 执行者负责；契约独立模块、Compose、运行验证与交付文档由主 Agent 负责。
